# 현재 트래커

> 최종 업데이트: 2026-09-14. 작업 재개·우선순위 선택에 사용한다. 상세 결과는 [DEVLOG](DEVLOG.md), 제품 범위·일정·후속 트랙은 [MASTERPLAN](MASTERPLAN.md)을 참조한다.

## 현재 목표와 중단점

- M0 운영 정리: 공통 지침, 조건별 문서 읽기/갱신, 선택형 검증, 실험 코드 보관 위치를 적용했다. 게임 코드·승격 기준·공개판은 변경하지 않았다.
- M1은 첫 플레이 가능한 Night BR 종료 판정 중이다. 개발/수동/후보 표면은 `data/mapSpec_night_forest_expanded_candidate.json`의 `night_br_m1_60`으로 유지한다.
- E-082 완료: 2초 하한은 경기별 최초 비권총 기록의 **5-run 평균**이며 개별 경기/플레이어의 획득 지연 계약이 아니다. 초기 배치와 보관된 네 5-run 묶음을 대조했고 약 0.6초 단발만으로 스폰 변경을 정당화할 수 없다고 판단했다. 수치·스폰·런타임은 유지했다. [E-082 결과](DEVLOG.md#e-082-초기-접근-거리와-평균-하한-정합성)
- 다음 E-083은 E-081 물리 시계 후보만 켠 비계측 대조/후보 각 5-run(입력 41000-41004)으로 평균 first upgrade·duration·생존·성능을 비교한다. 과거 process 시계 자료를 후보 결과에 합산하지 않고, 단발 2초 미만만으로 반복 검증을 금지하지 않는다. 초기 snapshot·시계 모드·격리 출력은 기존 도구를 재사용한다.
- 시계 후보는 기본 OFF이며 새 5-run은 미실시다. 수동 성능/체감·기본값·새 EXE 승격은 보류한다. 시간 임의 보정·인위적 수집 지연·하한 완화로 통과시키지 않는다. E078 과거 gate FAIL도 소급 변경하지 않는다.

## 수동 검증과 릴리즈

- E-067 패키지 수동 검증 대기: clean 소스 `78b5180`, Windows `builds/playtest/E-067_78b5180/BattleCapsule_E067_78b5180.exe`, 메뉴 `v2.1.0-demo-dev | E-067`. macOS ZIP·checksum도 같은 폴더이며 실제 Mac은 미검증이다.
- 수동 확인: 낡은/표준 무기 구분, 재장전/탄약 없음 경고, 총·호환 탄약 확보와 생존·존 압력. E-064 자동 분포와 수동 체감의 괴리는 아직 해소되지 않았다.
- 첫 공개 범위: Windows x64, 오프라인 싱글플레이, 한국어, 키보드/마우스, 한 맵 무료 데모. 온라인·99명 기본값·macOS 정식 지원·신규 콘텐츠 확장은 보류한다.
- `v2.1.0-demo-dev`는 제한된 테스트 프리릴리즈다. 공개 stable `v2.0.0-pre-expansion`은 수동/패키지·M2/M3 gate 전 교체하지 않는다.
- 패키징·저장·브랜딩 기반 작업은 병행 가능하다. 정상 기록/배지 생성, 전체 매치/재시작, Forward+ 성능, clean artifact 재현성과 실제 Mac 실행은 자동 부팅만으로 검증됐다고 보지 않는다. [릴리즈 절차](reference/RELEASE.md)

## 우선순위와 주요 위험

1. E-083에서 물리 시계 후보의 반복 gameplay/성능 회귀를 판단한다. 기준값·기본 스폰을 맞춤 튜닝하지 않고 수동/기본값 승격은 별도로 유지한다.
2. E-067 수동 피드백을 반영해 M1 체감 판정으로 돌아간다. 단발 sanity를 5-run 또는 수동 승격으로 해석하지 않는다.
3. 이후 대표 건물/교전·야간/HUD 슬라이스, 첫 사용자 완주, Windows 공개 RC는 MASTERPLAN의 M2/M3 순서를 따른다.

현재 위험은 M1 생존·교전 압력, 화면 가독성, 수동 저장/패키지 검증 공백이다. 99봇은 구조 안전망이며 체감 목표가 아니다. 수치만 맞추는 전역 드랍 상향이나 신규 기능 확장으로 위험을 우회하지 않는다.

## 브랜치와 로컬 보존

- 마지막 확인된 푸시: E-077~E-081과 운영 개선까지 6개 커밋 → `Kyulhee/battle-capsule` master, 원격 SHA `630048a1b96920a9dcd8ea4c88d941fb26dd0da1`. 이후 E-082는 로컬 작업이며 이번 완료된 푸시에 포함되지 않는다.
- 기존 `.gitignore`, `asset_generator/`, `plan_report/`와 미추적 Godot UID는 보존한다. UID 추적 정책 통일·과거 원시 결과 정리는 이번 변경에 포함하지 않았다.
- 상세 작업 규칙은 [AGENTS](../AGENTS.md), 문서 소유/갱신 기준은 [DOCS_INDEX](DOCS_INDEX.md)에 둔다.

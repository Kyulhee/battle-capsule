# 현재 트래커

> 최종 업데이트: 2026-09-15. 작업 재개·우선순위 선택에 사용한다. 상세 결과는 [DEVLOG](DEVLOG.md), 제품 범위·일정·후속 트랙은 [MASTERPLAN](MASTERPLAN.md)을 참조한다.

## 현재 목표와 중단점

- M0 운영 정리: 공통 지침, 조건별 문서 읽기/갱신, 선택형 검증, 실험 코드 보관 위치를 적용했다. 게임 코드·승격 기준·공개판은 변경하지 않았다.
- M1은 첫 플레이 가능한 Night BR 종료 판정 중이다. 개발/수동/후보 표면은 `data/mapSpec_night_forest_expanded_candidate.json`의 `night_br_m1_60`으로 유지한다.
- E-087 완료: 기존 초기 사망/노출과 종료 표본에서 보급 부족보다 저체력 DISENGAGE의 엄폐 진행을 다음 확인 축으로 좁혔다. `pressure_no_target` 조기 종료가 기존 공간 목표 완료 규칙을 우회할 수 있는 코드 경로를 찾았지만, 실매치의 엄폐 목표 identity/사망 인과는 미확정이다. 새 런타임·계측·경기는 없고 E086 보급 순찰과 물리 시계는 기본 OFF다. [E-087 결과](DEVLOG.md#e-087-초기-사망과-엄폐-종료-경로-검토)
- 다음 E-088은 `survival_break + 선택한 엄폐 미도달(>2m) + 압력 재교전 분기의 탐색 결과 null` 조건을 실제 Bot handler의 소형 fixture로 먼저 재현한다. 재현되면 해당 null 경로만 기존 ammo/8초 timeout/zone 검사와 공간 완료 로직으로 이어지게 하는 기본 OFF 후보를 만든다. 일반 이탈·엄폐 없음/도달·유효 적 재교전·target 획득 거절은 보존한다. 관련 fixture 후 새 동일 소스 process 대조/후보 1쌍으로 실제 효과 방향을 확인하며 E083 대조는 새 소스에 재사용하지 않는다. 재현 실패·구조 회귀·생존 방향 근거 없음이면 5-run으로 확대하지 않는다. 속도/HP/드랍/존 변경이나 E078/E081 혼합은 하지 않는다.
- 시계 후보는 기본 OFF이며 수동 성능/체감·기본값·새 EXE 승격은 보류한다. 2초 하한은 5-run 평균이며 시간 보정·인위적 수집 지연·하한 완화는 하지 않는다. E078/E081 과거 단발 gate FAIL도 소급 변경하지 않는다.

## 수동 검증과 릴리즈

- E-067 패키지 수동 검증 대기: clean 소스 `78b5180`, Windows `builds/playtest/E-067_78b5180/BattleCapsule_E067_78b5180.exe`, 메뉴 `v2.1.0-demo-dev | E-067`. macOS ZIP·checksum도 같은 폴더이며 실제 Mac은 미검증이다.
- 수동 확인: 낡은/표준 무기 구분, 재장전/탄약 없음 경고, 총·호환 탄약 확보와 생존·존 압력. E-064 자동 분포와 수동 체감의 괴리는 아직 해소되지 않았다.
- 현재 보호된 수동 JSON은 빌드 식별자가 없어 E067 실행으로 확인되지 않았다. 이를 E067 수동 통과나 자동 60봇 비교군으로 사용하지 않는다.
- 첫 공개 범위: Windows x64, 오프라인 싱글플레이, 한국어, 키보드/마우스, 한 맵 무료 데모. 온라인·99명 기본값·macOS 정식 지원·신규 콘텐츠 확장은 보류한다.
- `v2.1.0-demo-dev`는 제한된 테스트 프리릴리즈다. 공개 stable `v2.0.0-pre-expansion`은 수동/패키지·M2/M3 gate 전 교체하지 않는다.
- 패키징·저장·브랜딩 기반 작업은 병행 가능하다. 정상 기록/배지 생성, 전체 매치/재시작, Forward+ 성능, clean artifact 재현성과 실제 Mac 실행은 자동 부팅만으로 검증됐다고 보지 않는다. [릴리즈 절차](reference/RELEASE.md)

## 우선순위와 주요 위험

1. E-088의 엄폐 미완료/null-target 조기 종료 조건만 재현·단일 후보화한다. 종료 규칙의 정합성과 실제 생존 개선은 별도로 판정한다.
2. E-067 수동 피드백을 반영해 M1 체감 판정으로 돌아간다. 단발 sanity를 5-run 또는 수동 승격으로 해석하지 않는다.
3. 이후 대표 건물/교전·야간/HUD 슬라이스, 첫 사용자 완주, Windows 공개 RC는 MASTERPLAN의 M2/M3 순서를 따른다.

현재 위험은 M1 생존·교전 압력, 화면 가독성, 수동 저장/패키지 검증 공백이다. 99봇은 구조 안전망이며 체감 목표가 아니다. 수치만 맞추는 전역 드랍 상향이나 신규 기능 확장으로 위험을 우회하지 않는다.

## 브랜치와 로컬 보존

- 마지막 확인된 푸시: E-085까지 `Kyulhee/battle-capsule` master, 원격 SHA `700f0ef81d4ae84b0f5bf49eb4c13ee98d8385e1`. E-086/E-087은 이후 로컬 작업이며 추가 푸시/패키지 변경은 하지 않았다.
- 기존 `.gitignore`, `asset_generator/`, `plan_report/`와 미추적 Godot UID는 보존한다. UID 추적 정책 통일·과거 원시 결과 정리는 이번 변경에 포함하지 않았다.
- 상세 작업 규칙은 [AGENTS](../AGENTS.md), 문서 소유/갱신 기준은 [DOCS_INDEX](DOCS_INDEX.md)에 둔다.

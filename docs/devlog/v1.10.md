# v1.10-dev Slice Summary

> Compact summary extracted from the full devlog. See [DEVLOG_full_2026-05-13.md](DEVLOG_full_2026-05-13.md) for exact historical entries.

## Status Clarification

- v1.10 is still in progress. The entries below are completed slices, not a completed Main slimdown release.
- `Main.gd` remains the orchestration root and still contains substantial UI, match setup, event, and state-wiring code.
- The v1.10 target is practical edit isolation: routine config, catalog, asset, and item-display work should avoid unrelated `Main.gd` sections.
- Before v1.11, finish or explicitly defer v1.10.x item/asset readability polish and complete at least one larger structural closure slice such as `MenuController` or `MatchBootstrap`.

## SupplyDropController Boundary

- Added `src/core/SupplyDropController.gd`.
- Moved supply telegraph timing, position roll, pillar progress, consumable cluster count, and cluster offsets out of `Main.gd`.
- `Main.gd` still owns supply state exposed to minimap and actual node creation.

## Regression Sentinel Summary

- Added regression sentinel reporting to `tools/analyze_results.py`.
- The current testing direction treats longer matches as acceptable unless combat/damage/perception loops collapse.

## Catalog/UI Data Boundaries

- `ArtifactCatalog`: starting artifact choice data and modifier definitions.
- `DifficultyCatalog`: difficulty UI labels, descriptions, and colors.
- `HelpCatalog`: How to Play sections and row data.
- `MenuIconFactory`: procedural menu/records/help icons and capsule logo.
- `HelpPanelBuilder`: How to Play scroll content rendering from `HelpCatalog` rows.
- `RecordsPanelBuilder`: Records tabs, clear action, and history row rendering from Telemetry history.

## Current v1.10.x Stabilization

- Item/Asset Readability Polish is now tracked as a v1.10.x sub-scope.
- Focus areas:
  - pickup label LOD
  - lower common pickup glow
  - focused pickup clarity
  - AssetCatalog fallback/export checks

## v1.10.1 Display Patch

- Focused pickup floor marker was toned down by reducing radius, alpha, and emission while preserving focus selection logic.
- Runtime death drops now use `DropDisplayCatalog` for weapon, ammo, and heal display names.
- Player death drops no longer create English pickup labels such as `Pistol`, `Shotgun`, `Assault Rifle Ammo`, `Health Potion`, or `MedKit`.
- Bot death drops now share the same display catalog instead of owning duplicate naming helpers.

## v1.10.2 Icon Sizing Patch

- `tools/sync_generated_icons.ps1` now supports per-icon `VisualScale` overrides and `-OnlyCategory` filtering.
- Runtime weapon icons were re-synced with weapon-only filtering.
- Pistol/knife were scaled down, while AR/shotgun/railgun were slightly expanded to reduce the optical size mismatch caused by square generated canvases.
- HUD slot rendering and pickup decal code were not changed.

## Bot Objective Awareness Patch

- Loot/objective chase now separates navigation direction from view direction: bots continue moving to the pickup while scanning around that objective heading.
- Footstep and ambient awareness can set scan alerts during loot chase, so nearby movement can pull the bot's view without canceling the route.
- Opportunistic loot can be interrupted by a fully revealed enemy, but recovery/combat-loot runs keep their objective; direct damage and loud-gunshot overrides remain the higher-priority interruption paths.
- Telemetry hook names and JSON schema were not changed.

## v1.10.3 Data/Description Value Binding First Pass

- Starting artifact descriptions are now generated from structured `mods` values in `ArtifactCatalog`.
- Red Trigger damage/spread values and Armor Sponge heal-to-shield amounts are read by both description generation and Player gameplay code.
- Zone Battery difficulty-specific regeneration is prepared through `ArtifactCatalog.prepare_for_difficulty()` instead of a one-off `Main.gd` mutation.
- `ItemDisplayFormatter` now centralizes pickup detail text and player HUD slot ammo text from `ItemData`, `StatsData`, and `WeaponSlotManager` slot state.
- Broader candidates remain difficulty text, mission/pressure text, and help rows where numeric values duplicate gameplay data.
- No large localization framework, rebalance, Telemetry schema change, or v1.11 artifact content was added.

## v1.10.5 HellEventController Runtime Split

- Added `src/core/HellEventController.gd` as the runtime boundary for Hell blackout and bombardment events.
- `Main.gd` now chooses the Hell modifier, wires the controller, and keeps the announcement UI; timer ticking, overlay flash, bomb marker creation, actor damage application, and Hell event Telemetry logging are delegated.
- The old local bombardment constants were moved out of `Main.gd` into the controller as one step toward keeping tuning values near their algorithm boundary.
- `Main.gd` line count after this split is 1693.
- No Hell balance values, combat behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.6 Menu UI Builder Split

- Added `DifficultySelectorBuilder` for main menu difficulty buttons, pressure opt-in checkbox UI, highlight updates, and tooltip rendering.
- Added `SettingsPanelBuilder` for the Settings modal layout and controls.
- `Main.gd` keeps difficulty state, pressure opt-in state, AudioServer/DisplayServer actions, and settings save/load; UI construction moved to helpers.
- `Main.gd` line count after this split is 1576.
- No difficulty values, settings file keys, gameplay behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.7 Result Panel Builder Split

- Added `src/ui/panels/ResultPanelBuilder.gd` as the first panel-specific builder in the new UI placement direction.
- Result panel layout, result buttons, and label population moved out of `Main.gd`.
- `Main.gd` still owns match finalization, mission evaluation, score calculation, Telemetry calls, and panel routing.
- `Main.gd` line count after this split was 1472.
- No score formula, mission evaluation, Telemetry hook names, JSON schema, or scene panel names were changed.

## v1.10.8 Artifact Selection Panel Builder Split

- Added `src/ui/panels/ArtifactSelectionPanelBuilder.gd`.
- Artifact selection overlay, card layout, skip button, and card button construction moved out of `Main.gd`.
- `Main.gd` still owns artifact catalog lookup, `_pending_artifact`, panel lifetime, and `start_game()` transition.
- `Main.gd` line count after this split was 1386.
- No artifact data, modifier values, description generation, apply behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.9 Hell Announcement Panel Builder Split

- Added `src/ui/panels/HellAnnouncementBuilder.gd`.
- Hell mode announcement overlay, card layout, penalty/event rows, and start button construction moved out of `Main.gd`.
- `Main.gd` still owns Hell modifier selection, pause/unpause state, active panel lifetime, and dismiss fade.
- `Main.gd` line count after this split was 1280.
- No Hell modifier descriptions, runtime behavior, pause/dismiss behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.10 MenuController Routing Split

- Added `src/ui/menu/MenuController.gd`.
- Panel visibility routing, main menu button wiring, dynamic Settings button insertion, and Records/Help close wiring moved out of `Main.gd`.
- `Main.gd` keeps callbacks, settings behavior, content builders, and a thin `_show_panel()` wrapper.
- `Main.gd` line count after this split was 1273.
- No scene node names, panel visibility rules, gameplay behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.11 MatchBootstrap First Pass

- Added `src/systems/match/MatchBootstrap.gd` as the first new `systems/match` helper.
- Zone controller creation/configuration, bonus mission tracker creation/selection, pressure flag initialization, and Hell modifier roll moved out of `Main.gd`.
- `Main.gd` still owns `zone`, `mission_tracker`, pressure fields, Hell runtime wiring, spawn calls, Telemetry start calls, and artifact application.
- `Main.gd` line count after this split was 1277 because explicit helper calls offset most of the removed inline code.
- No zone values, mission pool rules, pressure behavior, Hell modifier range, Telemetry hook names, or JSON schema were changed.

## v1.10.12 MatchTuning Config/CLI Split

- Added `src/systems/match/MatchTuning.gd` for match/zone tuning interpretation.
- `game_config` match/zone reads, clamp defaults, CLI match override parsing, and CLI difficulty parsing moved out of `Main.gd`.
- `Main.gd` keeps exported match state and applies the parsed tuning dictionary; `loot_spawner.configure_count()` stays in Main because the spawner remains Main-owned.
- `Main.gd` was 1277 lines after this split; this was a boundary cleanup rather than a line-count win.
- No config keys, CLI aliases, default values, difficulty indices, gameplay behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.13 MenuVisualBuilder Split

- Added `src/ui/MenuVisualBuilder.gd` for main/secondary menu presentation styling.
- Menu panel gradients, main menu noise overlay, capsule logo placement, and shared button style definitions moved out of `Main.gd`.
- `Main.gd` keeps only panel/button wiring and a thin `_apply_btn_style()` wrapper used by existing UI builder callbacks.
- `HelpPanelBuilder` still owns How to Play content; `MenuVisualBuilder` owns presentation styling only.
- `Main.gd` line count after this split is 1200.
- No scene node names, menu routing rules, settings behavior, gameplay behavior, Telemetry hook names, or JSON schema were changed.

## v1.10.14 Pause/Event Overlay Builder Split

- Added `src/ui/panels/PausePanelBuilder.gd` for the pause overlay and pause menu buttons.
- Added `src/ui/overlays/EventTextBuilder.gd` for transient event text label construction and fade-out tween setup.
- `Main.gd` keeps pause state, callbacks, and event signal handling while UI helpers own overlay node construction.
- `Main.gd` line count after this split is 1156.
- No pause state rules, button callbacks, menu routing, Hell event behavior, gameplay behavior, Telemetry hook names, or JSON schema were changed.

## Verification Rhythm

- For each narrow completed slice:
  - `git diff --check`
  - Godot headless quit
  - `python tools\simulate_matches.py 1`

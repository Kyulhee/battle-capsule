# v1.8-dev Summary

> Compact summary extracted from the full devlog. See [DEVLOG_full_2026-05-13.md](DEVLOG_full_2026-05-13.md) for exact historical entries.

## Expansion Foundation Start

- Added `src/core/GameConfig.gd` and `data/game_config.json`.
- Moved bot count, loot count, spawn radius, zone timing/damage, difficulty parameters, and Hell timers into config.
- Added command-line overrides for `bot_count=`, `loot_count=`, and `spawn_radius=`.

## Debug Foundation

- Added `DebugFlags` and `DebugOverlay`.
- Added runtime args such as `debug=true`, `debug_overlay=`, and `debug_flags=ai,perception,damage,loot,zone,nav`.
- Release default remains debug off.

## AssetCatalog Foundation

- Added `data/asset_catalog.json` and `src/core/AssetCatalog.gd`.
- Registered audio/icon/prop/cosmetic ID categories with fallback behavior.
- `SoundManager` checks catalog paths first and falls back to existing/procedural sounds.

## Runtime Icon Pass

- Added `tools/sync_generated_icons.ps1` to normalize selected generated icons from `asset_generator/expected_output/assets/icons` into `assets/icons`.
- Integrated core weapon, ammo, heal, armor, and artifact icons.
- Player HUD and pickup decals load catalog icons with fallback.

## LootSpawner Boundary

- Added `src/core/LootSpawner.gd` for POI hotspot registration, density-weighted position choice, and initial loot counts.
- `Main.gd` still owns Pickup node creation and Telemetry/game-state wiring.

## Verification

- Godot headless quit passed during the original work.
- `python tools\simulate_matches.py 1` passed during the original work.
- Hell and debug flag smoke tests passed during the original work.

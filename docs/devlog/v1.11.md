# v1.11-dev Slice Summary

## Status

- v1.11 is the subsystem directory and non-Main data-boundary version.
- `Main.gd` remains the match-global orchestrator and still owns zone, mission, player, alive count, difficulty, pressure flags, supply minimap state, and Telemetry hook calls.
- v1.11 work should move files and data ownership by small domain slices, preserving class names, public APIs, scene references, behavior, and Telemetry schema.

## v1.11.1 Hell Subsystem Directory First Pass

- Moved `HellEventController.gd` from `src/core/` to `src/systems/hell/`.
- Updated `Main.gd` preload path to the new system location.
- Kept `class_name HellEventController`, modifier ids, public methods, `event_text_requested`, and Telemetry event names unchanged.
- This is a path-ownership cleanup only. Hell bombardment/blackout tuning constants remain unchanged and are scoped for the next v1.11 slice.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.2 Hell Tuning Data Boundary

- Added `src/systems/hell/HellTuning.gd`.
- Expanded `data/game_config.json` `hell` from timer-only keys into structured `timers`, `blackout`, `bombardment`, `barrage`, `standard`, and `disc` sections.
- Added `GameConfig.hell_tuning()` for structured Hell config reads while keeping `hell_value()` as a compatibility accessor.
- `HellEventController.gd` now reads sanitized tuning values from `HellTuning` and keeps runtime algorithms, node creation, damage application, event text signals, and Telemetry event logging.
- Current fallback/default values are preserved.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.3 Zone Subsystem Directory First Pass

- Moved `ZoneController.gd` from `src/core/` to `src/systems/zone/` with its `.uid` file.
- Updated `Main.gd` preload path.
- Kept `class_name ZoneController`, public methods, `stage_advanced`, `zone_warning`, stage config behavior, shrink interpolation, outside damage, and pressure mission hook calls unchanged.
- `Main.gd` continues to own `zone`; Bot/Player/Minimap/DebugOverlay/WorldPresentationBuilder continue to read `main.zone`.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.4 Loot/Supply Subsystem Directory First Pass

- Moved `LootSpawner.gd` from `src/core/` to `src/systems/loot/`.
- Moved `SupplyDropController.gd` from `src/core/` to `src/systems/loot/`.
- Moved `LootSpawnDirector.gd` from `src/systems/match/` to `src/systems/loot/`.
- Updated `Main.gd` preload paths.
- Kept `class_name`s, public methods, pickup quantities, supply timing, supply pillar creation, supply Telemetry calls, and Main-owned supply minimap state unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.5 Mission/Pressure Descriptor Catalog First Pass

- Added `src/systems/mission/MissionCatalog.gd`.
- Moved bonus mission list construction and hard/Hell pressure descriptor pools out of `MissionTracker.gd`.
- Kept `MissionTracker.gd` as the public API and runtime state/evaluation owner: `get_all_missions()`, `get_hard_pool()`, `get_hell_pool()`, `filter_feasible()`, progress counters, HUD formatting, and badge persistence remain stable.
- Kept mission ids, pressure condition enum values, reward/penalty descriptors, effect ids, `Main.gd` pressure trigger flow, `MatchBootstrap.gd` mission selection flow, and Telemetry schema unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.6 Pressure HUD Formatter First Pass

- Added `src/systems/mission/MissionHudFormatter.gd`.
- Moved pressure HUD title/deadline/progress/reward/penalty string assembly out of `MissionTracker.gd`.
- `MissionTracker.gd` now keeps pressure counters/evaluation and passes an explicit counter snapshot plus condition id mapping to the formatter.
- Kept public `get_pressure_hud_text()`, Player HUD label flow, pressure descriptors, effect labels, pressure success/fail behavior, and Telemetry schema unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.7 Bonus Mission HUD Formatter First Pass

- Extended `MissionHudFormatter.gd` to format bonus mission HUD text from explicit context data.
- `MissionTracker.gd` now keeps Telemetry/player HP context gathering and delegates `get_hud_text()` string assembly.
- Preserved current mission HUD strings, including the no-Telemetry fallback for all-weapon mission text.
- Kept mission completion evaluation, early-fail checks, badge persistence, Player HUD label flow, and Telemetry schema unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.8 Mission Evaluator First Pass

- Added `src/systems/mission/MissionEvaluator.gd`.
- Moved bonus mission completion and early-fail condition checks out of `MissionTracker.gd`.
- `MissionTracker.gd` now keeps hooks/counters and builds explicit final-rank/player-HP/Telemetry context for evaluator calls.
- Kept public `evaluate()` and `get_early_fail_status()` APIs, mission completion rules, badge persistence, result flow, pressure evaluation, and Telemetry schema unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.9 MissionTracker System Path Move

- Moved `MissionTracker.gd` and its `.uid` from `src/core/` to `src/systems/mission/`.
- Updated `Main.gd` preload path.
- Kept `class_name MissionTracker`, public APIs, mission/pressure state ownership, hooks, descriptors, evaluation, HUD behavior, badge persistence, and Telemetry-facing flow unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.10 Pressure Condition Evaluator First Pass

- Added `src/systems/mission/PressureConditionEvaluator.gd`.
- Moved pressure descriptor feasibility and active pressure condition completion checks out of `MissionTracker.gd`.
- `MissionTracker.gd` keeps `PressureCondition` ids, active pressure state, counters, hooks, deadline timing, instant-fail flag, and public pressure APIs.
- Kept pressure success/fail behavior, pressure descriptor ids, Main pressure trigger/effect flow, and Telemetry schema unchanged.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.11 Mission Badge Store First Pass

- Added `src/systems/mission/MissionBadgeStore.gd`.
- Moved `user://achievements.json` badge persistence out of `MissionTracker.gd`.
- `MissionTracker.gd` keeps public `save_badge()`, `has_badge()`, and `load_achievements()` wrappers.
- Preserved achievement JSON path, `badges` array shape, duplicate-prevention behavior, result flow, and Telemetry schema.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.12 Mission Subsystem Closure Review

- Re-audited `src/systems/mission/MissionTracker.gd` after the mission catalog, HUD formatter, evaluator, pressure condition evaluator, badge store, and path ownership slices.
- `MissionTracker.gd` is 257 lines and intentionally retains active mission/pressure state, counters, hooks, public wrappers, pressure timing/instant-fail state, and context assembly.
- Split mission subsystem owners are now documented: `MissionCatalog.gd`, `MissionHudFormatter.gd`, `MissionEvaluator.gd`, `PressureConditionEvaluator.gd`, and `MissionBadgeStore.gd`.
- No runtime code, mission behavior, pressure behavior, or Telemetry schema changed.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only closure review.

## v1.11.13 Player HUD Builder First Pass

- Added `src/ui/player/PlayerHudBuilder.gd`.
- Moved Player top HUD construction/styling for the zone timer, mission HUD, pressure HUD, flash panel, and kill feed container out of `Player.gd`.
- `Player.gd` now keeps runtime label references and continues to own HUD value updates, flash/kill feed population, health/shield/stat/slot HUD, player state, combat, movement, and artifact behavior.
- Preserved the existing CanvasLayer child order for the extracted nodes and did not change mission/pressure text, Telemetry schema, combat constants, or slot behavior.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.14 Player HUD/Status Builder Continuation

- Extended `src/ui/player/PlayerHudBuilder.gd` to build health/shield rows, status counters, artifact label, bottom slot HUD nodes, and the zone warning overlay.
- `Player.gd` now keeps references returned by the builder and continues to own runtime health/shield/stat value updates, slot state styling, ammo text, weapon icon loading/fallbacks, combat, movement, and artifact behavior.
- Removed Player-local status icon helper functions by moving them into the HUD builder.
- Preserved existing CanvasLayer child order and did not change slot behavior, weapon icon lookup, mission/pressure text, Telemetry schema, or combat constants.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.15 Player Slot HUD Renderer

- Added `src/ui/player/PlayerSlotHudRenderer.gd`.
- Moved active/normal/out-of-ammo slot panel styling, slot ammo text, and ammo warning colors out of `Player.gd`.
- `Player.gd` now delegates `_refresh_slot_hud()` to the renderer while keeping `WeaponSlotManager` ownership, slot signal wiring, reload-progress override text, weapon icon loading/fallback generation, combat, movement, and artifact behavior.
- The renderer receives weapon icon lookup through an explicit `Callable` and does not read `AssetCatalog` or the scene tree.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.16 Player Weapon Icon Resolver

- Added `src/ui/player/PlayerWeaponIconResolver.gd`.
- Moved weapon HUD icon cache, AssetCatalog path loading, image-file fallback loading, and procedural pixel fallback icon generation out of `Player.gd`.
- `Player.gd` now passes `main.asset_catalog` explicitly through the slot HUD renderer icon callback while keeping scene-tree lookup and slot/inventory ownership.
- Preserved weapon icon ids, fallback shapes/colors, slot HUD icon behavior, and missing-asset fallback behavior.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.17 Player Tuning Constants Boundary

- Added `src/entities/player/PlayerTuning.gd`.
- Moved footstep interval, heal regen rate, shot heat/spread tuning, melee tuning, and occluder fade tuning constants out of `Player.gd`.
- `Player.gd` now references `PlayerTuning.gd` while keeping movement/combat/heal/occluder algorithms, runtime state, artifact modifiers, slot state, and HUD value updates.
- Preserved all current numeric values exactly and did not introduce JSON loading in this slice.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.18 Player Occluder Fader Helper

- Added `src/entities/player/PlayerOccluderFader.gd`.
- Moved camera-to-player occluder ray sampling, occluder mesh discovery, fade material state, and restore behavior out of `Player.gd`.
- `Player.gd` now delegates wall transparency updates with `self` and the camera node while keeping camera lookup, movement/combat/heal state, artifact modifiers, HUD updates, and zone warning logic.
- Kept occluder tuning values in `PlayerTuning.gd` and preserved ray sample points, collision mask, fade material behavior, linger timing, and exit-tree restore behavior.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.19 Player Pass Closure Review

- Re-audited `src/entities/player/Player.gd` after the HUD builder, slot renderer, weapon icon resolver, tuning constants, and occluder fader slices.
- `Player.gd` is 832 lines and intentionally retains movement/input/crouch/footstep execution, health/shield runtime updates, heal consumption/regeneration, combat firing/melee execution, artifact modifier application, pickup focus/interaction, kill feed population, zone warning update, and Sfx/Telemetry hooks.
- Split Player-side owners are now documented: `PlayerHudBuilder.gd`, `PlayerSlotHudRenderer.gd`, `PlayerWeaponIconResolver.gd`, `PlayerTuning.gd`, and `PlayerOccluderFader.gd`.
- No runtime code or Telemetry schema changed.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only closure review.

## v1.11.20 Bot Tuning Constants Boundary

- Added `src/entities/bot/BotTuning.gd`.
- Moved melee, attack-bout reposition, retreat counterfire, Hard gunshot awareness, and debug marker constants out of `Bot.gd`.
- `Bot.gd` now references `BotTuning.gd` while keeping AI state machine, target selection, movement, combat, recovery/disengage behavior, perception checks, loot decisions, archetype/difficulty runtime state, and Telemetry hooks.
- Preserved all current numeric/debug values exactly.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.21 Bot Debug Label Builder

- Added `src/entities/bot/BotDebugLabelBuilder.gd`.
- Moved state label and archetype marker `Label3D` construction/styling out of `Bot.gd`.
- `Bot.gd` now keeps label references and continues to own state label text, archetype marker text/color, visibility, reveal checks, AI state machine, combat, perception, and Telemetry hooks.
- Preserved label positions, sizes, outline sizes, billboard/double-sided settings, and initial hidden state.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.22 Bot Marker Formatter

- Added `src/entities/bot/BotMarkerFormatter.gd`.
- Moved state label specs, archetype marker text, combat-plan marker abbreviations, archetype fallback colors, and cosmetic catalog ids out of `Bot.gd`.
- `Bot.gd` now keeps `Label3D` references, marker visibility, reveal checks, AssetCatalog lookup, visual skin application, AI state machine, combat, perception, and Telemetry hooks.
- Preserved current marker text, colors, catalog ids, and visibility behavior.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.23 Bot Visual Skin Controller

- Added `src/entities/bot/BotVisualSkinController.gd`.
- Moved `ArchetypeSkin` root apply/sync/hide lifecycle out of `Bot.gd`.
- `Bot.gd` now delegates skin root visibility, crouch offset/scale sync, and death hiding while keeping crouch decisions, body mesh updates, AssetCatalog lookup, doctrine profile application, AI behavior, and Telemetry hooks.
- `BotVisualKit.gd` now reuses `BotMarkerFormatter.gd` cosmetic catalog ids so marker tint and skin tint id mapping has one owner.
- Preserved current skin node name, skin part geometry, catalog tint behavior, crouch offsets/scales, and death hiding behavior.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.24 Bot Pass Closure Review

- Re-audited `src/entities/bot/Bot.gd` after the tuning, debug label builder, marker formatter, and visual skin controller slices.
- `Bot.gd` is 1908 lines and intentionally retains AI state machine/runtime timers, navigation/stuck handling, objective/loot/supply decisions, recovery/disengage/zone escape behavior, perception/noise/ambient awareness checks, combat movement and firing/melee execution, damage/death/drop handling, crouch body mesh updates, Sfx/Telemetry hooks, and Main-owned zone/alive-count/AssetCatalog reads.
- Split Bot-side owners are now documented: `BotDoctrine.gd`, `BotTuning.gd`, `BotDebugLabelBuilder.gd`, `BotMarkerFormatter.gd`, `BotVisualKit.gd`, and `BotVisualSkinController.gd`.
- No runtime code, AI behavior, visual behavior, or Telemetry schema changed.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only closure review.

## v1.11.25 Entity Data-Boundary Planning

- Audited remaining post-Player/Bot candidates for data/algorithm separation.
- Selected `WeaponSlotManager.gd` as the next concrete slice because reload times and reserve-ammo caps are still direct code values while inventory arrays, reload state, public APIs, and algorithms can remain stable.
- Planned v1.11.26 as a small `WeaponSlotTuning` helper pass that preserves existing values and avoids JSON migration in the first pass.
- Deferred `Pickup.gd` presentation splitting until after weapon tuning because it mixes mesh/glow/label/icon concerns with collection side effects.
- Deferred broader mission numeric prose/data migration until new mission content is planned.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only planning slice.

## v1.11.26 Weapon Slot Tuning Boundary

- Added `src/core/WeaponSlotTuning.gd`.
- Moved weapon reload times, no-weapon/unknown-weapon reload fallbacks, reserve-ammo caps, and reserve fallback out of `WeaponSlotManager.gd`.
- `WeaponSlotManager.gd` now keeps slot arrays, active slot, reload timers, reload transfer state, signals, public APIs, and inventory/reload algorithms.
- Preserved current values exactly: no-weapon reload 1.5s, unknown-weapon reload 1.3s, shotgun 2.8s, railgun 4.5s, AR 2.0s, reserve caps pistol 30 / AR 60 / shotgun 12 / railgun 4 / fallback 30.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.27 Pickup Presentation Boundary

- Added `src/entities/pickup/PickupPresentation.gd`.
- Moved pickup base colors, glow/light tuning, label LOD distances, focused/normal label scale, label colors, visibility refresh interval, and icon plane size/height values out of `Pickup.gd`.
- `Pickup.gd` now keeps runtime node creation, focus/LOS updates, cluster-label comparison, AssetCatalog icon loading, item collection side effects, Telemetry pickup logging, and debug logging.
- Preserved current colors, glow/light values, label distances/scales/colors, icon plane sizes/heights, item collection behavior, and Telemetry schema.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.28 Pickup Icon Resolver

- Added `src/entities/pickup/PickupIconResolver.gd`.
- Moved pickup icon ids, per-pickup texture cache, AssetCatalog icon path lookup, ResourceLoader texture loading, and image-file fallback loading out of `Pickup.gd`.
- `Pickup.gd` now keeps AssetCatalog scene lookup, icon decal node creation/material placement, focus/LOS updates, collection side effects, Telemetry pickup logging, and debug logging.
- Preserved current icon ids, cache behavior, catalog path lookup, image fallback behavior, icon material setup, item collection behavior, and Telemetry schema.
- Verified with `git diff --check`, Godot headless quit, one normal simulation, and one Hell simulation.

## v1.11.29 Pickup Pass Closure Review

- Re-audited `src/entities/pickup/Pickup.gd` after the presentation and icon resolver slices.
- `Pickup.gd` is 307 lines and intentionally retains runtime mesh/light/label/focus/icon node creation, focus/LOS refresh, cluster-label comparison, AssetCatalog scene lookup, icon decal material placement, item collection side effects for weapon/ammo/heal/armor, Telemetry pickup logging, debug logging, and queue-free lifecycle.
- Split Pickup-side owners are now documented: `ItemDisplayFormatter.gd`, `PickupPresentation.gd`, and `PickupIconResolver.gd`.
- No runtime code, pickup behavior, visual behavior, or Telemetry schema changed.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only closure review.

## v1.11.30 Mission Numeric Description Audit

- Audited bonus mission, pressure mission, HUD, and pressure effect text for duplicated gameplay numbers.
- Found the safest next slice in bonus mission descriptions: `MissionCatalog.gd` duplicates `MissionData.target_value` in prose while `MissionEvaluator.gd` already evaluates from `target_value`.
- Noted remaining HUD hardcoded prose fragments such as `90초`, `12m`, and implicit "1 kill each" text.
- Deferred pressure mission descriptor description generation because those descriptors combine multiple conditions, rewards, penalties, instant-fail flags, and feasibility rules.
- Planned v1.11.31 as a focused bonus mission description formatter/builder pass.
- Verified with `git diff --check` and Godot headless quit. Runtime simulation skipped because this is a docs-only audit/planning slice.

## v1.11.31 Bonus Mission Description Formatter

- Added `src/systems/mission/MissionTuning.gd` for shared mission thresholds: supply-kill radius, full detection threshold, detected/heavily-detected bot counts, low-HP kill ratio, and all-weapon kill requirements.
- Added `src/systems/mission/MissionDescriptionFormatter.gd` for generated bonus mission descriptions and shared weapon labels.
- `MissionCatalog.gd` now sets mission ids/titles/target values/badge data first, then fills `description` through the formatter.
- `MissionHudFormatter.gd`, `MissionEvaluator.gd`, `MissionTracker.gd`, and `Main.gd` now reference `MissionData.target_value` / `MissionTuning.gd` instead of duplicating `90초`, `12m`, detected bot counts, one-slot limits, and all-weapon kill targets.
- Pressure mission descriptor descriptions remain deferred to a later pressure-focused slice because they combine multiple conditions, rewards, penalties, and instant-fail flags.
- Preserved mission ids, target values, badge data, pressure descriptors, mission/pressure runtime flow, and Telemetry schema.
- Verified with `git diff --check`, Godot headless quit, one normal simulation (duration=76.8s, stage=3), and one Hell simulation (duration=53.3s, stage=2).

## v1.11.32 Pressure Mission Description Formatter

- Added `src/systems/mission/PressureMissionDescriptionFormatter.gd` for pressure descriptor descriptions generated from `conditions[]`.
- `MissionCatalog.gd` now defines pressure descriptor ids/titles/conditions/rewards/penalties/instant-fail flags first, then fills `description` through `_with_pressure_descriptions()`.
- `MissionTuning.gd` now also exposes the low-HP percentage label used by pressure condition text.
- Preserved pressure ids, condition arrays, reward/penalty descriptors, instant-fail flags, feasibility rules, pressure HUD layout, condition evaluation, reward/penalty execution, and Telemetry schema.
- Verified with `git diff --check`, Godot headless quit, one normal simulation (duration=52.5s, stage=2), and one Hell simulation (duration=89.5s, stage=3).

## Planned Follow-Ups

- v1.11.33: pressure feasibility tuning boundary.
- Later v1.11 slices: remaining subsystem data/algorithm boundaries.

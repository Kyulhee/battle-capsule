# v2.0-dev Summary

> Last updated: 2026-06-01.

## Intent

v2.0 prepares the game for larger maps and higher player counts by making map scale, match scale, spawn/loot density, zone profile, minimap/full-map metadata, and selected visual props explicit data before tuning toward 99 players.

## Boundary Rules

- `Main.gd` remains the match-global orchestrator and state owner.
- `MapDefinition.gd` owns definition data and validation, not scene mutation.
- `WorldBuilder.gd` continues to build world geometry from explicit spec data and must not trust imported GLB collision as gameplay authority.
- `Minimap.gd` and future Full Map UI render map/zone/marker data only; they do not own match logic.
- Scale increases must use validated presets before 99-player tuning.

## v2.0.1 MapDefinition Compatibility Loader

- Added `src/core/MapDefinition.gd` as a compatibility wrapper over current `MapSpec` JSON.
- Supports legacy MapSpec JSON directly and a wrapper format with `map_spec`, `match`, `runtime`, `zone`, and `scale_presets` sections.
- Exposes sanitized match, runtime, and zone tuning merged with `GameConfig`-compatible fallbacks.
- Adds validation for id/name, world size, POI bounds, obstacle bounds, spawn radius sanity, and loot-capable POI coverage.
- Added `tools/verify_map_definition.gd` to pin legacy wrapping and wrapper-format match overrides.

**Verification**

- `verify_map_definition.gd` passed: `mountain_forest_alpha`, 7 POIs, 35 obstacles, wrapper bot override 24.
- Godot headless project load passed with expected AssetCatalog warning only.
- One normal simulation passed.

## v2.0.2 Definition-Driven Scale Preset Merge

- Added baseline match/runtime/zone values and `baseline` / `medium_24` scale presets to `data/mapSpec_example.json`.
- `Main.gd` now loads `MapDefinition` before world generation and applies the selected scale preset into match tuning and runtime tuning.
- `MatchRuntimeTuning.gd` and `LootSpawner.gd` now support `hotspot_density_mult` for preset-driven POI loot density scaling.
- `MatchTuning.gd` accepts `scale_preset` / `map_scale_preset` command-line args for smoke scale runs.
- `tools/verify_map_definition.gd` verifies preset match overrides and runtime loot density overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.
- `scale_preset=medium_24` normal simulation passed with 24 bots.

## v2.0.3 Read-Only Full Map UI Foundation

- Added `src/ui/FullMapOverlay.gd` as a presentation-only overlay for world bounds, POIs, generated obstacle footprints, current/next zone, player facing, and supply marker state.
- `Main.gd` now syncs map UI data through `_sync_map_views()` and toggles Full Map with `M`; `ESC` closes the overlay before pause.
- The overlay consumes `MapDefinition`, wrapped `MapSpec`, and `WorldBuilder.get_minimap_features()` data without owning match logic.
- Added `tools/verify_full_map_overlay.gd` for layout bounds, world projection, generated feature ingestion, and runtime marker smoke coverage.

**Verification**

- `verify_full_map_overlay.gd` passed.
- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.4 MapDefinition Validation Expansion

- Expanded `MapDefinition.validate()` for POI density/bias sanity, oversized POI radii, rotated obstacle footprint bounds, spawn radius plus runtime clearance, loot density coverage, and implicit zone radius sanity.
- Added summary fields for loot hotspot count and resolved zone initial radius.
- Added flat match-key fallback support for legacy scale preset dictionaries while keeping nested `match` sections as the preferred format.
- Extended `tools/verify_map_definition.gd` with negative validation probes for POI, obstacle, spawn, and zone failures.

**Verification**

- `verify_map_definition.gd` passed.

## v2.0.5 SettingsManager Boundary Extraction

- Added `src/core/SettingsManager.gd` for `user://settings.cfg` load/save, master volume application, fullscreen state, clamping, and runtime sync.
- `Main.gd` now keeps settings panel callbacks only and delegates settings persistence/audio/display mutation to `SettingsManager`.
- Added `tools/verify_settings_manager.gd` for save/load, clamp, fullscreen state, and missing-file fallback smoke coverage.

**Verification**

- `verify_settings_manager.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.

## v2.0.6 Conservative 40-Bot Scale Preset Smoke

- Added `large_40` as the next scale preset after `medium_24`.
- The preset uses 40 bots, 104 loot, 56m spawn radius, 1.12 hotspot density multiplier, and slightly longer zone timings.
- `tools/verify_map_definition.gd` now verifies `large_40` match, runtime loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=large_40` normal simulation passed with 40 bots.

## v2.0.7 MapDefinition Position Query Compatibility

- Added `MapDefinition` helpers for world size/bounds, in-bounds checks, clamping, bounds UV conversion, and distance scaling.
- Added defensive POI and obstacle descriptor accessors with typed position/scale/extent fields for presentation consumers.
- `FullMapOverlay` and `Minimap` now prefer `MapDefinition` query data while preserving legacy `MapSpec` fallback paths.

**Verification**

- `verify_map_definition.gd` passed with query/descriptor checks.
- `verify_full_map_overlay.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.8 Conservative 60-Bot Scale Feasibility

- Added `xlarge_60` as the next scale preset after `large_40`.
- The preset uses 60 bots, 144 loot, 56m spawn radius, 80 safe spawn attempts, 1.15 hotspot density multiplier, and 22/36/26 zone timings.
- `tools/verify_map_definition.gd` now verifies `xlarge_60` match, runtime spawn/loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=xlarge_60` normal simulation passed with 60 bots.
- Baseline normal simulation passed.

## v2.0.9 60-Bot Telemetry Repeat And Scale Decision

- Ran 5 repeated `scale_preset=xlarge_60` normal simulations and analyzed `tools/sim_runs_current`.
- Regression sentinels were clean: zero damage, zero weapon damage, zero shot, and zero combat plan all reported `none`.
- Telemetry summary: avg duration 93.2s, no runs under 60s, recover success 230/527 (43.6%), died in RECOVER 1 total.
- Scale warning: avg first upgrade was 9.5s, with high disengage/stuck/survival volume, so 99-player tuning remains blocked.

**Decision**

- Do not add a 99-player preset yet.
- Next work should tighten `xlarge_60` spawn/loot distribution and repeated-run telemetry gates before larger scale.

## v2.0.10 60-Bot Distribution Tuning And Telemetry Gate

- Retuned current `xlarge_60` to 120 loot, 1.05 hotspot density multiplier, 0.08/0.08 stage wave probabilities, 6x stage wave count multiplier, and 30s initial zone timer.
- Added `tools/check_scale_telemetry.py` to fail repeated scale runs on zero combat sentinels, duration floor, first-upgrade pacing, recover deaths, stuck volume, and disengage volume.
- Kept 99-player tuning blocked; `xlarge_60` remains the active test surface.

**Verification**

- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 100.2s, avg first upgrade 11.1s, and no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed.
- Godot headless project load and baseline normal simulation passed.

## Planned Follow-Ups

- v2.0.11: add AI update-budget / LOD telemetry at 60-bot scale before 99-player tuning.
- Later scale presets: 11 -> 24 -> 40 -> 60 -> 99 with AI LOD and telemetry checks at each step.

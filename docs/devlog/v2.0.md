# v2.0-dev Summary

> Last updated: 2026-06-02.

## Intent

v2.0 prepares the game for larger maps and higher player counts by making map scale, match scale, spawn/loot density, zone profile, minimap/full-map metadata, and selected visual props explicit data before tuning toward 99 players.

## Boundary Rules

- `Main.gd` remains the match-global orchestrator and state owner.
- `MapDefinition.gd` owns definition data and validation, not scene mutation.
- `WorldBuilder.gd` continues to build world geometry from explicit spec data and must not trust imported GLB collision as gameplay authority.
- `Minimap.gd` and future Full Map UI render map/zone/marker data only; they do not own match logic.
- Scale increases must use validated presets before 99-player tuning.

## v2.0.1 MapDefinition Compatibility Loader

- Added `src/core/MapDefinition.gd` as a compatibility wrapper over current `MapSpec` JSON.
- Supports legacy MapSpec JSON directly and a wrapper format with `map_spec`, `match`, `runtime`, `zone`, and `scale_presets` sections.
- Exposes sanitized match, runtime, and zone tuning merged with `GameConfig`-compatible fallbacks.
- Adds validation for id/name, world size, POI bounds, obstacle bounds, spawn radius sanity, and loot-capable POI coverage.
- Added `tools/verify_map_definition.gd` to pin legacy wrapping and wrapper-format match overrides.

**Verification**

- `verify_map_definition.gd` passed: `mountain_forest_alpha`, 7 POIs, 35 obstacles, wrapper bot override 24.
- Godot headless project load passed with expected AssetCatalog warning only.
- One normal simulation passed.

## v2.0.2 Definition-Driven Scale Preset Merge

- Added baseline match/runtime/zone values and `baseline` / `medium_24` scale presets to `data/mapSpec_example.json`.
- `Main.gd` now loads `MapDefinition` before world generation and applies the selected scale preset into match tuning and runtime tuning.
- `MatchRuntimeTuning.gd` and `LootSpawner.gd` now support `hotspot_density_mult` for preset-driven POI loot density scaling.
- `MatchTuning.gd` accepts `scale_preset` / `map_scale_preset` command-line args for smoke scale runs.
- `tools/verify_map_definition.gd` verifies preset match overrides and runtime loot density overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.
- `scale_preset=medium_24` normal simulation passed with 24 bots.

## v2.0.3 Read-Only Full Map UI Foundation

- Added `src/ui/FullMapOverlay.gd` as a presentation-only overlay for world bounds, POIs, generated obstacle footprints, current/next zone, player facing, and supply marker state.
- `Main.gd` now syncs map UI data through `_sync_map_views()` and toggles Full Map with `M`; `ESC` closes the overlay before pause.
- The overlay consumes `MapDefinition`, wrapped `MapSpec`, and `WorldBuilder.get_minimap_features()` data without owning match logic.
- Added `tools/verify_full_map_overlay.gd` for layout bounds, world projection, generated feature ingestion, and runtime marker smoke coverage.

**Verification**

- `verify_full_map_overlay.gd` passed.
- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.4 MapDefinition Validation Expansion

- Expanded `MapDefinition.validate()` for POI density/bias sanity, oversized POI radii, rotated obstacle footprint bounds, spawn radius plus runtime clearance, loot density coverage, and implicit zone radius sanity.
- Added summary fields for loot hotspot count and resolved zone initial radius.
- Added flat match-key fallback support for legacy scale preset dictionaries while keeping nested `match` sections as the preferred format.
- Extended `tools/verify_map_definition.gd` with negative validation probes for POI, obstacle, spawn, and zone failures.

**Verification**

- `verify_map_definition.gd` passed.

## v2.0.5 SettingsManager Boundary Extraction

- Added `src/core/SettingsManager.gd` for `user://settings.cfg` load/save, master volume application, fullscreen state, clamping, and runtime sync.
- `Main.gd` now keeps settings panel callbacks only and delegates settings persistence/audio/display mutation to `SettingsManager`.
- Added `tools/verify_settings_manager.gd` for save/load, clamp, fullscreen state, and missing-file fallback smoke coverage.

**Verification**

- `verify_settings_manager.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.

## v2.0.6 Conservative 40-Bot Scale Preset Smoke

- Added `large_40` as the next scale preset after `medium_24`.
- The preset uses 40 bots, 104 loot, 56m spawn radius, 1.12 hotspot density multiplier, and slightly longer zone timings.
- `tools/verify_map_definition.gd` now verifies `large_40` match, runtime loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=large_40` normal simulation passed with 40 bots.

## v2.0.7 MapDefinition Position Query Compatibility

- Added `MapDefinition` helpers for world size/bounds, in-bounds checks, clamping, bounds UV conversion, and distance scaling.
- Added defensive POI and obstacle descriptor accessors with typed position/scale/extent fields for presentation consumers.
- `FullMapOverlay` and `Minimap` now prefer `MapDefinition` query data while preserving legacy `MapSpec` fallback paths.

**Verification**

- `verify_map_definition.gd` passed with query/descriptor checks.
- `verify_full_map_overlay.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.8 Conservative 60-Bot Scale Feasibility

- Added `xlarge_60` as the next scale preset after `large_40`.
- The preset uses 60 bots, 144 loot, 56m spawn radius, 80 safe spawn attempts, 1.15 hotspot density multiplier, and 22/36/26 zone timings.
- `tools/verify_map_definition.gd` now verifies `xlarge_60` match, runtime spawn/loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=xlarge_60` normal simulation passed with 60 bots.
- Baseline normal simulation passed.

## v2.0.9 60-Bot Telemetry Repeat And Scale Decision

- Ran 5 repeated `scale_preset=xlarge_60` normal simulations and analyzed `tools/sim_runs_current`.
- Regression sentinels were clean: zero damage, zero weapon damage, zero shot, and zero combat plan all reported `none`.
- Telemetry summary: avg duration 93.2s, no runs under 60s, recover success 230/527 (43.6%), died in RECOVER 1 total.
- Scale warning: avg first upgrade was 9.5s, with high disengage/stuck/survival volume, so 99-player tuning remains blocked.

**Decision**

- Do not add a 99-player preset yet.
- Next work should tighten `xlarge_60` spawn/loot distribution and repeated-run telemetry gates before larger scale.

## v2.0.10 60-Bot Distribution Tuning And Telemetry Gate

- Retuned current `xlarge_60` to 120 loot, 1.05 hotspot density multiplier, 0.08/0.08 stage wave probabilities, 6x stage wave count multiplier, and 30s initial zone timer.
- Added `tools/check_scale_telemetry.py` to fail repeated scale runs on zero combat sentinels, duration floor, first-upgrade pacing, recover deaths, stuck volume, and disengage volume.
- Kept 99-player tuning blocked; `xlarge_60` remains the active test surface.

**Verification**

- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 100.2s, avg first upgrade 11.1s, and no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed.
- Godot headless project load and baseline normal simulation passed.

## v2.0.11 AI Update Budget Telemetry Probe

- Added sampled bot AI update-budget telemetry from `Bot.gd` into `Telemetry.gd`.
- Simulation results now include an `ai` group with update sample count, total/max update time, and state/archetype buckets.
- `tools/analyze_results.py` reports AI update budget and top state averages.
- `tools/check_scale_telemetry.py` prints the AI budget in the repeated 60-bot gate and applies loose guardrails when samples are present.
- No AI LOD behavior or 99-player preset was added; this slice only creates the measurement backbone.

**Verification**

- Python compile checks passed for the analyzer and scale gate.
- Godot headless project load passed with expected AssetCatalog warning only.
- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 97.3s, avg first upgrade 12.6s, no zero damage/shot/plan sentinels, and AI update budget samples=25210, avg=363.3us, max=28459us.
- `check_scale_telemetry.py` passed.

## v2.0.12 60-Bot Progressive Zone Pacing

- Added explicit `xlarge_60` zone stage overrides so 60-bot scale no longer inherits the much faster baseline stage 2+ timings.
- The 60-bot curve now keeps early compression slower, then accelerates through later stages: stage 2 is 30s wait / 24s shrink, then later stages move to 22/18, 14/12, and 10/10.
- The first shrink also starts slightly later and shrinks slightly longer via `initial_timer=34` and `shrink_time=28`.
- No 99-player preset, new map, or AI LOD behavior was added.

**Verification**

- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 103.9s, avg zone stage 2.00, avg first upgrade 11.8s, no zero damage/shot/plan sentinels, and AI update budget avg=369.8us.
- `check_scale_telemetry.py` passed.

## v2.0.13 60-Bot Spawn Distribution Telemetry

- Added a `spawn` telemetry group for requested/placed entity count, fallback count, nearest-spawn distance, spawn attempt count, and annulus saturation.
- `Main.gd` now includes fallback spawn positions in `_spawn_positions` so later placement checks and telemetry reflect actual fallback placement.
- `tools/analyze_results.py` reports spawn distribution, and `tools/check_scale_telemetry.py` gates fallback usage and minimum nearest spacing when spawn data exists.
- No 99-player preset or larger map was added.

**Verification**

- Python compile checks passed for the analyzer and scale gate.
- Godot headless project load passed with expected AssetCatalog warning only.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported placed=61/61, fallback=0.0/run, min nearest=3.5m, avg nearest=7.1m, avg attempts=1.5, max attempts=7, saturation=0.24.
- `check_scale_telemetry.py` passed.

**Decision**

- The current 120m map / 56m spawn radius is viable for 60 bots, but it is already using the clearance floor.
- 99-player tuning remains blocked until a larger map/spawn envelope plan exists.

## v2.0.14 Larger-Map Spawn Envelope Prerequisite

- Added `scale_envelopes.target_99` to the map definition as a planning target rather than a runtime scale preset.
- The 99-bot prerequisite envelope is pinned at minimum 160m world / 72m spawn radius and preferred 180m world / 78m spawn radius.
- `MapDefinition.gd` now loads, exposes, summarizes, and sanity-checks `scale_envelopes`.
- Added `tools/verify_scale_envelope.gd` to verify the target envelope math against current `xlarge_60` and ensure `target_99` is not accidentally promoted to `scale_presets`.

**Verification**

- `verify_map_definition.gd` passed.
- `verify_scale_envelope.gd` passed with current `xlarge_60` saturation=0.24 / boundary margin=0.5m and `target_99` min=160m + 72m / preferred=180m + 78m.

**Decision**

- 99-player tuning remains blocked until a new or rescaled map spec satisfies the `target_99` envelope.
- Next implementation work should create a non-default larger map candidate or map-rescale prototype before any 99-player runtime preset.

## v2.0.15 Non-Default Large Map Candidate

- Added `data/mapSpec_large_candidate.json` as a separate 180m forest candidate map.
- The default runtime map path remains unchanged; this candidate is not loaded by `Main.gd` yet.
- The candidate has 11 POIs, 35 obstacles, a 60-bot `xlarge_60` test surface, 150 loot, 78m spawn radius, and 120 safe spawn attempts.
- `target_99` remains a `scale_envelope`, not a playable `scale_preset`; the candidate exposes only `baseline` and `xlarge_60`.
- Added `tools/verify_large_map_candidate.gd` to validate the candidate against `MapDefinition`, the preferred target envelope, boundary margin, POI/obstacle counts, and annulus saturation.

**Verification**

- `verify_large_map_candidate.gd` passed with world=180m, spawn=78m, boundary margin=8.5m, and target_99 preferred saturation=0.20.
- `verify_map_definition.gd` and `verify_scale_envelope.gd` passed.
- Godot headless project load passed with the expected AssetCatalog fallback warning only.
- `git diff --check` passed.

**Decision**

- This slice creates the concrete data backbone for larger-map work, not a 99-player playable preset.
- v2.0.16 should add a safe runtime loading/smoke path for the non-default candidate before any `target_99` preset is considered.

## v2.0.16 Candidate Map Runtime Loading Smoke

- Added `map_spec_path` as an exported `Main.gd` map source path with the existing default `res://data/mapSpec_example.json`.
- Added `map_spec_path`, `map_spec`, `map_definition_path`, and `map_definition` command-line aliases through `MatchTuning.gd`.
- Changed command-line parsing so resource path values preserve original casing while existing difficulty/scale preset parsing still normalizes where useful.
- Added `tools/verify_map_runtime_path.gd` to pin CLI path parsing and candidate `xlarge_60` summary resolution.

**Verification**

- `verify_map_runtime_path.gd` passed.
- Godot headless load with `map_spec_path=res://data/mapSpec_large_candidate.json scale_preset=xlarge_60` loaded `Mountain Forest Alpha Large Candidate`.
- 5-run candidate-map `xlarge_60` simulation passed: avg duration 99.7s, avg zone stage 2.00, no runs under 60s, no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed on the candidate run set: fallback=0.0/run, min nearest=3.5m, avg nearest=9.6m, saturation=0.12, AI avg=311.8us.

**Decision**

- The larger candidate can now be runtime-smoked without changing the default map.
- v2.0.17 may introduce a guarded 99-target probe on the candidate path only, but default map behavior and release scope remain unchanged.

## v2.0.17 Guarded 99-Target Candidate Probe

- Added `target_99_probe` only to `data/mapSpec_large_candidate.json`.
- The default map still exposes no 99-player runtime preset, and `target_99` remains a planning `scale_envelope`.
- The candidate-only probe uses 99 bots, 240 loot, 78m spawn radius, 180 safe spawn attempts, 8m inner radius, and slower early zone pacing.
- Added `tools/verify_candidate_99_probe.gd` to pin candidate-only availability, default-map absence, envelope fit, and probe tuning floors.

**Verification**

- `verify_candidate_99_probe.gd`, `verify_large_map_candidate.gd`, `verify_map_definition.gd`, and `verify_map_runtime_path.gd` passed.
- 5-run candidate-map `target_99_probe` simulation passed with avg duration 129.4s, avg zone stage 2.00, no runs under 60s, and no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed: fallback=0.0/run, min nearest=3.5m, avg nearest=7.7m, attempts=1.3/5 max, saturation=0.20, AI avg=439.9us.

**Decision**

- 99 bots are technically smokeable on the non-default candidate path.
- v2.0.18 should analyze and tune 99-probe pacing, zone escape volume, and engagement density before any default/global promotion.

## v2.0.18 99-Probe Scale-Normalized Analysis

- Extended `tools/analyze_results.py` to print rates per spawned entity/minute for damage, shots, combat plans, disengage, stuck, zone fire, and survival breaks.
- Added aggregate doctrine state mix reporting so larger runs can be compared by state share instead of only raw state seconds or raw event counts.
- Kept this as analysis tooling only; no gameplay values, default map path, or global 99-player availability changed.

**Verification**

- `python -m py_compile tools\analyze_results.py` passed.
- `python tools\analyze_results.py tools\sim_runs_current` passed on the current 99-probe telemetry set.
- Current 99-probe normalized output: damage=27.7, shots=3.35, plans=1.83, disengage=0.56, stuck=0.11, zone_fire=1.02, survival=1.24 per spawned entity/min.
- Current 99-probe state mix: ZONE_ESCAPE 26.0%, DISENGAGE 22.0%, CHASE 19.2%, ATTACK 18.9%, IDLE 14.0%.

**Decision**

- 99-probe promotion remains blocked until the same normalized analysis is compared against candidate `xlarge_60`.
- The first review targets are zone escape share and disengage share, not spawn placement or AI update cost.

## v2.0.19 Normalized 60-vs-99 Candidate Comparison

- Added `out_dir=` / `sim_out_dir=` handling to `tools/simulate_matches.py` so comparison runs can write to separate directories, including `C:\tmp`.
- Added `tools/compare_scale_profiles.py` to compare two run directories across spawn distribution, per spawned entity/minute rates, AI update budget, and doctrine state mix.
- Ran fresh candidate-map 5-run sets for `xlarge_60` and `target_99_probe` outside the repo.

**Verification**

- Python compile checks passed for `simulate_matches.py`, `compare_scale_profiles.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- Candidate `xlarge_60` gate passed from `C:\tmp\game_dev_candidate_60`: avg duration 124.7s, fallback=0.0/run, min nearest=3.8m, saturation=0.12, AI avg=295.6us.
- Candidate `target_99_probe` gate passed from `C:\tmp\game_dev_candidate_99`: avg duration 131.8s, fallback=0.0/run, min nearest=3.5m, saturation=0.20, AI avg=419.4us.
- Comparison deltas for 99 vs 60: duration +7.1s, first upgrade +1.5s, damage/entity/min -4.12, plans/entity/min -0.86, ZONE_ESCAPE +2.14pp, DISENGAGE +5.32pp.

**Decision**

- The candidate-only 99 probe is not currently blocked by spawn placement or AI update budget.
- Promotion remains blocked by design review: DISENGAGE share and ZONE_ESCAPE share need targeted analysis before tuning.

## v2.0.20 99-Probe Pressure Decision

- Extended `tools/compare_scale_profiles.py` with `DISENGAGE sec/trigger` and a pressure-decision summary.
- The summary separates spawn/pathing blockers, AI budget blockers, DISENGAGE trigger-frequency pressure, DISENGAGE duration/exit pressure, and ZONE_ESCAPE pressure.
- Reviewed current bot flow: DISENGAGE has multiple sources, but current persisted tactics telemetry does not preserve reason counts for several existing call sites.
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- The comparison tool passed on the fresh candidate-map 60-vs-99 directories.
- Current decision output: spawn/pathing is not the blocker, AI budget is not the blocker, DISENGAGE pressure looks duration/exit-related rather than trigger-frequency-related, and ZONE_ESCAPE should be reviewed after DISENGAGE exit behavior.

**Decision**

- Do not change zone profile, spawn/POI density, or outnumbered thresholds yet.
- v2.0.21 should add persisted DISENGAGE reason telemetry, then rerun candidate 60-vs-99 comparison.

## v2.0.21 Persisted DISENGAGE Reason Telemetry

- Added persisted `tactics.disengage_entries`, `tactics.disengage_reasons`, and `tactics.disengage_reasons_by_archetype`.
- Instrumented DISENGAGE entry reasons from current bot flow: `outnumbered`, `losing_fight`, `reload_retreat`, `sniper_min_range`, `attack_timeout`, and `survival_break`.
- Preserved `disengage_triggered` as the existing outnumbered/legacy trigger metric used by scale gates; full entry volume is now reported separately.
- Extended `tools/analyze_results.py` with average DISENGAGE entries, reason counts/rates, and reason-by-archetype output.
- Extended `tools/compare_scale_profiles.py` with `disengage entries/entity/min`, `DISENGAGE sec/entry`, and reason-rate deltas.
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- 1-run candidate-map `xlarge_60` smoke wrote persisted reason telemetry to `C:\tmp\game_dev_disengage_reason_smoke`.
- `check_scale_telemetry.py --min-runs 1` passed on the smoke output, confirming existing gate semantics still read the legacy trigger count.
- `compare_scale_profiles.py` printed reason-rate rows when comparing the smoke output against itself.

**Decision**

- Do not tune DISENGAGE, zone pacing, spawn/POI density, or outnumbered thresholds yet.
- v2.0.22 should rerun fresh 5-run candidate `xlarge_60` and `target_99_probe` sets and compare reason-aware deltas before behavior changes.

## Planned Follow-Ups

- v2.0.22: rerun candidate 60-vs-99 comparison with persisted DISENGAGE reason telemetry before tuning.
- Later scale presets: 11 -> 24 -> 40 -> 60 -> 99 with AI LOD and telemetry checks at each step.

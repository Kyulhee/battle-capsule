# v2.0-dev Summary

> Last updated: 2026-06-04.

## Intent

v2.0 prepares the game for larger maps and higher player counts by making map scale, match scale, spawn/loot density, zone profile, minimap/full-map metadata, and selected visual props explicit data before tuning toward 99 players.

## Boundary Rules

- `Main.gd` remains the match-global orchestrator and state owner.
- `MapDefinition.gd` owns definition data and validation, not scene mutation.
- `WorldBuilder.gd` continues to build world geometry from explicit spec data and must not trust imported GLB collision as gameplay authority.
- `Minimap.gd` and future Full Map UI render map/zone/marker data only; they do not own match logic.
- Scale increases must use validated presets before 99-player tuning.

## v2.0.1 MapDefinition Compatibility Loader

- Added `src/core/MapDefinition.gd` as a compatibility wrapper over current `MapSpec` JSON.
- Supports legacy MapSpec JSON directly and a wrapper format with `map_spec`, `match`, `runtime`, `zone`, and `scale_presets` sections.
- Exposes sanitized match, runtime, and zone tuning merged with `GameConfig`-compatible fallbacks.
- Adds validation for id/name, world size, POI bounds, obstacle bounds, spawn radius sanity, and loot-capable POI coverage.
- Added `tools/verify_map_definition.gd` to pin legacy wrapping and wrapper-format match overrides.

**Verification**

- `verify_map_definition.gd` passed: `mountain_forest_alpha`, 7 POIs, 35 obstacles, wrapper bot override 24.
- Godot headless project load passed with expected AssetCatalog warning only.
- One normal simulation passed.

## v2.0.2 Definition-Driven Scale Preset Merge

- Added baseline match/runtime/zone values and `baseline` / `medium_24` scale presets to `data/mapSpec_example.json`.
- `Main.gd` now loads `MapDefinition` before world generation and applies the selected scale preset into match tuning and runtime tuning.
- `MatchRuntimeTuning.gd` and `LootSpawner.gd` now support `hotspot_density_mult` for preset-driven POI loot density scaling.
- `MatchTuning.gd` accepts `scale_preset` / `map_scale_preset` command-line args for smoke scale runs.
- `tools/verify_map_definition.gd` verifies preset match overrides and runtime loot density overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.
- `scale_preset=medium_24` normal simulation passed with 24 bots.

## v2.0.3 Read-Only Full Map UI Foundation

- Added `src/ui/FullMapOverlay.gd` as a presentation-only overlay for world bounds, POIs, generated obstacle footprints, current/next zone, player facing, and supply marker state.
- `Main.gd` now syncs map UI data through `_sync_map_views()` and toggles Full Map with `M`; `ESC` closes the overlay before pause.
- The overlay consumes `MapDefinition`, wrapped `MapSpec`, and `WorldBuilder.get_minimap_features()` data without owning match logic.
- Added `tools/verify_full_map_overlay.gd` for layout bounds, world projection, generated feature ingestion, and runtime marker smoke coverage.

**Verification**

- `verify_full_map_overlay.gd` passed.
- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.4 MapDefinition Validation Expansion

- Expanded `MapDefinition.validate()` for POI density/bias sanity, oversized POI radii, rotated obstacle footprint bounds, spawn radius plus runtime clearance, loot density coverage, and implicit zone radius sanity.
- Added summary fields for loot hotspot count and resolved zone initial radius.
- Added flat match-key fallback support for legacy scale preset dictionaries while keeping nested `match` sections as the preferred format.
- Extended `tools/verify_map_definition.gd` with negative validation probes for POI, obstacle, spawn, and zone failures.

**Verification**

- `verify_map_definition.gd` passed.

## v2.0.5 SettingsManager Boundary Extraction

- Added `src/core/SettingsManager.gd` for `user://settings.cfg` load/save, master volume application, fullscreen state, clamping, and runtime sync.
- `Main.gd` now keeps settings panel callbacks only and delegates settings persistence/audio/display mutation to `SettingsManager`.
- Added `tools/verify_settings_manager.gd` for save/load, clamp, fullscreen state, and missing-file fallback smoke coverage.

**Verification**

- `verify_settings_manager.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- Baseline normal simulation passed.

## v2.0.6 Conservative 40-Bot Scale Preset Smoke

- Added `large_40` as the next scale preset after `medium_24`.
- The preset uses 40 bots, 104 loot, 56m spawn radius, 1.12 hotspot density multiplier, and slightly longer zone timings.
- `tools/verify_map_definition.gd` now verifies `large_40` match, runtime loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=large_40` normal simulation passed with 40 bots.

## v2.0.7 MapDefinition Position Query Compatibility

- Added `MapDefinition` helpers for world size/bounds, in-bounds checks, clamping, bounds UV conversion, and distance scaling.
- Added defensive POI and obstacle descriptor accessors with typed position/scale/extent fields for presentation consumers.
- `FullMapOverlay` and `Minimap` now prefer `MapDefinition` query data while preserving legacy `MapSpec` fallback paths.

**Verification**

- `verify_map_definition.gd` passed with query/descriptor checks.
- `verify_full_map_overlay.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.

## v2.0.8 Conservative 60-Bot Scale Feasibility

- Added `xlarge_60` as the next scale preset after `large_40`.
- The preset uses 60 bots, 144 loot, 56m spawn radius, 80 safe spawn attempts, 1.15 hotspot density multiplier, and 22/36/26 zone timings.
- `tools/verify_map_definition.gd` now verifies `xlarge_60` match, runtime spawn/loot, and zone overrides.

**Verification**

- `verify_map_definition.gd` passed.
- Godot headless project load passed with expected AssetCatalog warning only.
- `scale_preset=xlarge_60` normal simulation passed with 60 bots.
- Baseline normal simulation passed.

## v2.0.9 60-Bot Telemetry Repeat And Scale Decision

- Ran 5 repeated `scale_preset=xlarge_60` normal simulations and analyzed `tools/sim_runs_current`.
- Regression sentinels were clean: zero damage, zero weapon damage, zero shot, and zero combat plan all reported `none`.
- Telemetry summary: avg duration 93.2s, no runs under 60s, recover success 230/527 (43.6%), died in RECOVER 1 total.
- Scale warning: avg first upgrade was 9.5s, with high disengage/stuck/survival volume, so 99-player tuning remains blocked.

**Decision**

- Do not add a 99-player preset yet.
- Next work should tighten `xlarge_60` spawn/loot distribution and repeated-run telemetry gates before larger scale.

## v2.0.10 60-Bot Distribution Tuning And Telemetry Gate

- Retuned current `xlarge_60` to 120 loot, 1.05 hotspot density multiplier, 0.08/0.08 stage wave probabilities, 6x stage wave count multiplier, and 30s initial zone timer.
- Added `tools/check_scale_telemetry.py` to fail repeated scale runs on zero combat sentinels, duration floor, first-upgrade pacing, recover deaths, stuck volume, and disengage volume.
- Kept 99-player tuning blocked; `xlarge_60` remains the active test surface.

**Verification**

- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 100.2s, avg first upgrade 11.1s, and no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed.
- Godot headless project load and baseline normal simulation passed.

## v2.0.11 AI Update Budget Telemetry Probe

- Added sampled bot AI update-budget telemetry from `Bot.gd` into `Telemetry.gd`.
- Simulation results now include an `ai` group with update sample count, total/max update time, and state/archetype buckets.
- `tools/analyze_results.py` reports AI update budget and top state averages.
- `tools/check_scale_telemetry.py` prints the AI budget in the repeated 60-bot gate and applies loose guardrails when samples are present.
- No AI LOD behavior or 99-player preset was added; this slice only creates the measurement backbone.

**Verification**

- Python compile checks passed for the analyzer and scale gate.
- Godot headless project load passed with expected AssetCatalog warning only.
- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 97.3s, avg first upgrade 12.6s, no zero damage/shot/plan sentinels, and AI update budget samples=25210, avg=363.3us, max=28459us.
- `check_scale_telemetry.py` passed.

## v2.0.12 60-Bot Progressive Zone Pacing

- Added explicit `xlarge_60` zone stage overrides so 60-bot scale no longer inherits the much faster baseline stage 2+ timings.
- The 60-bot curve now keeps early compression slower, then accelerates through later stages: stage 2 is 30s wait / 24s shrink, then later stages move to 22/18, 14/12, and 10/10.
- The first shrink also starts slightly later and shrinks slightly longer via `initial_timer=34` and `shrink_time=28`.
- No 99-player preset, new map, or AI LOD behavior was added.

**Verification**

- `verify_map_definition.gd` passed.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported avg duration 103.9s, avg zone stage 2.00, avg first upgrade 11.8s, no zero damage/shot/plan sentinels, and AI update budget avg=369.8us.
- `check_scale_telemetry.py` passed.

## v2.0.13 60-Bot Spawn Distribution Telemetry

- Added a `spawn` telemetry group for requested/placed entity count, fallback count, nearest-spawn distance, spawn attempt count, and annulus saturation.
- `Main.gd` now includes fallback spawn positions in `_spawn_positions` so later placement checks and telemetry reflect actual fallback placement.
- `tools/analyze_results.py` reports spawn distribution, and `tools/check_scale_telemetry.py` gates fallback usage and minimum nearest spacing when spawn data exists.
- No 99-player preset or larger map was added.

**Verification**

- Python compile checks passed for the analyzer and scale gate.
- Godot headless project load passed with expected AssetCatalog warning only.
- 5-run `scale_preset=xlarge_60` simulation passed.
- `analyze_results.py` reported placed=61/61, fallback=0.0/run, min nearest=3.5m, avg nearest=7.1m, avg attempts=1.5, max attempts=7, saturation=0.24.
- `check_scale_telemetry.py` passed.

**Decision**

- The current 120m map / 56m spawn radius is viable for 60 bots, but it is already using the clearance floor.
- 99-player tuning remains blocked until a larger map/spawn envelope plan exists.

## v2.0.14 Larger-Map Spawn Envelope Prerequisite

- Added `scale_envelopes.target_99` to the map definition as a planning target rather than a runtime scale preset.
- The 99-bot prerequisite envelope is pinned at minimum 160m world / 72m spawn radius and preferred 180m world / 78m spawn radius.
- `MapDefinition.gd` now loads, exposes, summarizes, and sanity-checks `scale_envelopes`.
- Added `tools/verify_scale_envelope.gd` to verify the target envelope math against current `xlarge_60` and ensure `target_99` is not accidentally promoted to `scale_presets`.

**Verification**

- `verify_map_definition.gd` passed.
- `verify_scale_envelope.gd` passed with current `xlarge_60` saturation=0.24 / boundary margin=0.5m and `target_99` min=160m + 72m / preferred=180m + 78m.

**Decision**

- 99-player tuning remains blocked until a new or rescaled map spec satisfies the `target_99` envelope.
- Next implementation work should create a non-default larger map candidate or map-rescale prototype before any 99-player runtime preset.

## v2.0.15 Non-Default Large Map Candidate

- Added `data/mapSpec_large_candidate.json` as a separate 180m forest candidate map.
- The default runtime map path remains unchanged; this candidate is not loaded by `Main.gd` yet.
- The candidate has 11 POIs, 35 obstacles, a 60-bot `xlarge_60` test surface, 150 loot, 78m spawn radius, and 120 safe spawn attempts.
- `target_99` remains a `scale_envelope`, not a playable `scale_preset`; the candidate exposes only `baseline` and `xlarge_60`.
- Added `tools/verify_large_map_candidate.gd` to validate the candidate against `MapDefinition`, the preferred target envelope, boundary margin, POI/obstacle counts, and annulus saturation.

**Verification**

- `verify_large_map_candidate.gd` passed with world=180m, spawn=78m, boundary margin=8.5m, and target_99 preferred saturation=0.20.
- `verify_map_definition.gd` and `verify_scale_envelope.gd` passed.
- Godot headless project load passed with the expected AssetCatalog fallback warning only.
- `git diff --check` passed.

**Decision**

- This slice creates the concrete data backbone for larger-map work, not a 99-player playable preset.
- v2.0.16 should add a safe runtime loading/smoke path for the non-default candidate before any `target_99` preset is considered.

## v2.0.16 Candidate Map Runtime Loading Smoke

- Added `map_spec_path` as an exported `Main.gd` map source path with the existing default `res://data/mapSpec_example.json`.
- Added `map_spec_path`, `map_spec`, `map_definition_path`, and `map_definition` command-line aliases through `MatchTuning.gd`.
- Changed command-line parsing so resource path values preserve original casing while existing difficulty/scale preset parsing still normalizes where useful.
- Added `tools/verify_map_runtime_path.gd` to pin CLI path parsing and candidate `xlarge_60` summary resolution.

**Verification**

- `verify_map_runtime_path.gd` passed.
- Godot headless load with `map_spec_path=res://data/mapSpec_large_candidate.json scale_preset=xlarge_60` loaded `Mountain Forest Alpha Large Candidate`.
- 5-run candidate-map `xlarge_60` simulation passed: avg duration 99.7s, avg zone stage 2.00, no runs under 60s, no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed on the candidate run set: fallback=0.0/run, min nearest=3.5m, avg nearest=9.6m, saturation=0.12, AI avg=311.8us.

**Decision**

- The larger candidate can now be runtime-smoked without changing the default map.
- v2.0.17 may introduce a guarded 99-target probe on the candidate path only, but default map behavior and release scope remain unchanged.

## v2.0.17 Guarded 99-Target Candidate Probe

- Added `target_99_probe` only to `data/mapSpec_large_candidate.json`.
- The default map still exposes no 99-player runtime preset, and `target_99` remains a planning `scale_envelope`.
- The candidate-only probe uses 99 bots, 240 loot, 78m spawn radius, 180 safe spawn attempts, 8m inner radius, and slower early zone pacing.
- Added `tools/verify_candidate_99_probe.gd` to pin candidate-only availability, default-map absence, envelope fit, and probe tuning floors.

**Verification**

- `verify_candidate_99_probe.gd`, `verify_large_map_candidate.gd`, `verify_map_definition.gd`, and `verify_map_runtime_path.gd` passed.
- 5-run candidate-map `target_99_probe` simulation passed with avg duration 129.4s, avg zone stage 2.00, no runs under 60s, and no zero damage/shot/plan sentinels.
- `check_scale_telemetry.py` passed: fallback=0.0/run, min nearest=3.5m, avg nearest=7.7m, attempts=1.3/5 max, saturation=0.20, AI avg=439.9us.

**Decision**

- 99 bots are technically smokeable on the non-default candidate path.
- v2.0.18 should analyze and tune 99-probe pacing, zone escape volume, and engagement density before any default/global promotion.

## v2.0.18 99-Probe Scale-Normalized Analysis

- Extended `tools/analyze_results.py` to print rates per spawned entity/minute for damage, shots, combat plans, disengage, stuck, zone fire, and survival breaks.
- Added aggregate doctrine state mix reporting so larger runs can be compared by state share instead of only raw state seconds or raw event counts.
- Kept this as analysis tooling only; no gameplay values, default map path, or global 99-player availability changed.

**Verification**

- `python -m py_compile tools\analyze_results.py` passed.
- `python tools\analyze_results.py tools\sim_runs_current` passed on the current 99-probe telemetry set.
- Current 99-probe normalized output: damage=27.7, shots=3.35, plans=1.83, disengage=0.56, stuck=0.11, zone_fire=1.02, survival=1.24 per spawned entity/min.
- Current 99-probe state mix: ZONE_ESCAPE 26.0%, DISENGAGE 22.0%, CHASE 19.2%, ATTACK 18.9%, IDLE 14.0%.

**Decision**

- 99-probe promotion remains blocked until the same normalized analysis is compared against candidate `xlarge_60`.
- The first review targets are zone escape share and disengage share, not spawn placement or AI update cost.

## v2.0.19 Normalized 60-vs-99 Candidate Comparison

- Added `out_dir=` / `sim_out_dir=` handling to `tools/simulate_matches.py` so comparison runs can write to separate directories, including `C:\tmp`.
- Added `tools/compare_scale_profiles.py` to compare two run directories across spawn distribution, per spawned entity/minute rates, AI update budget, and doctrine state mix.
- Ran fresh candidate-map 5-run sets for `xlarge_60` and `target_99_probe` outside the repo.

**Verification**

- Python compile checks passed for `simulate_matches.py`, `compare_scale_profiles.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- Candidate `xlarge_60` gate passed from `C:\tmp\game_dev_candidate_60`: avg duration 124.7s, fallback=0.0/run, min nearest=3.8m, saturation=0.12, AI avg=295.6us.
- Candidate `target_99_probe` gate passed from `C:\tmp\game_dev_candidate_99`: avg duration 131.8s, fallback=0.0/run, min nearest=3.5m, saturation=0.20, AI avg=419.4us.
- Comparison deltas for 99 vs 60: duration +7.1s, first upgrade +1.5s, damage/entity/min -4.12, plans/entity/min -0.86, ZONE_ESCAPE +2.14pp, DISENGAGE +5.32pp.

**Decision**

- The candidate-only 99 probe is not currently blocked by spawn placement or AI update budget.
- Promotion remains blocked by design review: DISENGAGE share and ZONE_ESCAPE share need targeted analysis before tuning.

## v2.0.20 99-Probe Pressure Decision

- Extended `tools/compare_scale_profiles.py` with `DISENGAGE sec/trigger` and a pressure-decision summary.
- The summary separates spawn/pathing blockers, AI budget blockers, DISENGAGE trigger-frequency pressure, DISENGAGE duration/exit pressure, and ZONE_ESCAPE pressure.
- Reviewed current bot flow: DISENGAGE has multiple sources, but current persisted tactics telemetry does not preserve reason counts for several existing call sites.
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- The comparison tool passed on the fresh candidate-map 60-vs-99 directories.
- Current decision output: spawn/pathing is not the blocker, AI budget is not the blocker, DISENGAGE pressure looks duration/exit-related rather than trigger-frequency-related, and ZONE_ESCAPE should be reviewed after DISENGAGE exit behavior.

**Decision**

- Do not change zone profile, spawn/POI density, or outnumbered thresholds yet.
- v2.0.21 should add persisted DISENGAGE reason telemetry, then rerun candidate 60-vs-99 comparison.

## v2.0.21 Persisted DISENGAGE Reason Telemetry

- Added persisted `tactics.disengage_entries`, `tactics.disengage_reasons`, and `tactics.disengage_reasons_by_archetype`.
- Instrumented DISENGAGE entry reasons from current bot flow: `outnumbered`, `losing_fight`, `reload_retreat`, `sniper_min_range`, `attack_timeout`, and `survival_break`.
- Preserved `disengage_triggered` as the existing outnumbered/legacy trigger metric used by scale gates; full entry volume is now reported separately.
- Extended `tools/analyze_results.py` with average DISENGAGE entries, reason counts/rates, and reason-by-archetype output.
- Extended `tools/compare_scale_profiles.py` with `disengage entries/entity/min`, `DISENGAGE sec/entry`, and reason-rate deltas.
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- 1-run candidate-map `xlarge_60` smoke wrote persisted reason telemetry to `C:\tmp\game_dev_disengage_reason_smoke`.
- `check_scale_telemetry.py --min-runs 1` passed on the smoke output, confirming existing gate semantics still read the legacy trigger count.
- `compare_scale_profiles.py` printed reason-rate rows when comparing the smoke output against itself.

**Decision**

- Do not tune DISENGAGE, zone pacing, spawn/POI density, or outnumbered thresholds yet.
- v2.0.22 should rerun fresh 5-run candidate `xlarge_60` and `target_99_probe` sets and compare reason-aware deltas before behavior changes.

## v2.0.22 Reason-Aware 60-vs-99 Candidate Comparison

- Ran fresh 5-run candidate-map sets with persisted DISENGAGE reason telemetry:
  - `C:\tmp\game_dev_reason_candidate_60` using `xlarge_60`.
  - `C:\tmp\game_dev_reason_candidate_99` using `target_99_probe`.
- Both sets passed `tools/check_scale_telemetry.py`.
- Compared the sets with `tools/compare_scale_profiles.py`.
- Kept gameplay unchanged.

**Verification**

- `xlarge_60`: avg duration 106.3s, first upgrade 12.8s, fallback=0.0/run, min nearest=3.6m, AI avg=361.8us.
- `target_99_probe`: avg duration 148.0s, first upgrade 27.4s, fallback=0.0/run, min nearest=3.6m, AI avg=504.3us.
- 99-vs-60 deltas: duration +41.8s, first upgrade +14.5s, damage/entity/min -11.63, shots/entity/min -1.42, plans/entity/min -0.99, ZONE_ESCAPE +0.26pp, DISENGAGE +4.25pp.
- Reason-aware deltas: DISENGAGE entries/entity/min -1.20, survival_break -0.97, outnumbered -0.21, `DISENGAGE sec/entry` +0.25.
- The 99 legacy `disengage` gate passed but is tight: avg 129.0 against the current max 130.0.

**Decision**

- DISENGAGE state share is higher at 99 despite lower normalized entry/reason rates, so do not tune outnumbered thresholds yet.
- The stronger issue is 99-probe tempo dilution: first upgrades are later, damage/shots/plans per entity/min are lower, and matches last much longer.
- v2.0.23 should diagnose economy/combat tempo on the 99 candidate before changing zone pacing, spawn/POI density, or bot behavior thresholds.

## v2.0.23 99-Probe Economy/Combat Tempo Diagnosis

- Extended `tools/compare_scale_profiles.py` with economy-normalized rows for weapon pickups, non-pistol pickups, rare pickups, heals, and shields per spawned entity/minute.
- Added a `Tempo decision` summary that separates economy dilution, combat throughput dilution, and stretched match duration.
- Extended `tools/analyze_results.py` with economy-normalized per spawned entity/minute output for single run directories.
- Reused the fresh v2.0.22 candidate sets; no gameplay data changed.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- `git diff --check` passed.
- Fresh 60-vs-99 comparison now reports economy tempo deltas: weapon pickups/entity/min -0.15, non-pistol pickups/entity/min -0.07, rare pickups/entity/min -0.12.
- Tempo decision reports economy dilution, combat throughput dilution, and materially stretched match duration.
- `analyze_results.py` prints economy-normalized rows; 99 probe shows weapons=0.39, non_pistol=0.04, rare=0.06 per entity/min.

**Decision**

- 99-probe pressure should not be tuned first through DISENGAGE thresholds.
- Raw loot count is close to 60 density, but actual non-pistol and rare pickup access is much worse at 99.
- v2.0.24 should try a candidate-only loot/economy adjustment focused on non-pistol/rare access, then rerun the same gates and reason-aware comparison.

## v2.0.24 Candidate-Only 99-Probe Loot/Economy Adjustment

- Added runtime loot `rare_bias_mult` support with default `1.0`.
- `LootSpawner` now multiplies POI `rare_bias` by runtime `rare_bias_mult` before clamping, which affects initial weapon chance and wave weapon selection.
- Applied the knob only to `target_99_probe` in `data/mapSpec_large_candidate.json`.
- Final v3 adjustment:
  - `hotspot_density_mult`: 1.12 -> 1.16.
  - `rare_bias_mult`: 1.45.
  - `loot_count` remains 240.
  - `stage_wave_count_mult` remains 9.
- Updated `verify_candidate_99_probe.gd` to guard the economy probe floor.

**Verification**

- `verify_candidate_99_probe.gd`, `verify_large_map_candidate.gd`, and `verify_map_runtime_path.gd` passed.
- Adjusted v3 99 set at `C:\tmp\game_dev_candidate_99_loot_v3` passed `check_scale_telemetry.py`: avg duration 149.0s, first upgrade 16.6s, legacy disengage 125.6/run, AI avg 546.5us, fallback 0.0/run.
- v3 vs previous 99: first upgrade -10.8s, non-pistol pickups/entity/min +0.03, rare pickups/entity/min +0.03, DISENGAGE state -0.68pp.
- v3 vs 60: first upgrade gap is now +3.75s, but combat throughput remains low: damage/entity/min -12.67, shots/entity/min -1.51, plans/entity/min -1.06, duration +42.7s.

**Decision**

- Keep v3 as the candidate-only economy adjustment.
- Do not keep the broader discarded v2 direction that increased raw loot/wave volume; it improved pickup rates but worsened duration and combat throughput.
- v2.0.25 should diagnose 99 combat throughput/engagement density before zone pacing or bot-threshold tuning.

## v2.0.25 99 Combat Throughput / Engagement Density Diagnosis

- Extended `tools/compare_scale_profiles.py` with engagement-density diagnostics:
  - damage/shots/plans per match minute.
  - damage/shots per `ATTACK` minute.
  - engage samples per spawned entity/minute.
  - combined `ATTACK+CHASE` and `DISENGAGE+ZONE_ESCAPE` state share.
  - `Engagement density decision`.
- Extended `tools/analyze_results.py` with matching single-directory engagement-density output.
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- `git diff --check` passed.
- Fresh 60 vs adjusted 99 v3 comparison now reports:
  - entities 61 -> 100 (+64%).
  - damage/match min 2238.3 -> 2402.1 (+7%).
  - shots/match min 273.3 -> 296.5 (+8%).
  - plans/match min 169.5 -> 172.0 (+1%).
  - damage/ATTACK min 770.5 -> 819.3.
  - shots/ATTACK min 94.1 -> 101.1.
  - `ATTACK+CHASE` 41.32% -> 37.67%; `RETREAT+ESCAPE` 44.13% -> 48.79%.

**Decision**

- 99 combat throughput is not scaling with population.
- Once a bot reaches `ATTACK`, lethality is intact or slightly better; the blocker is engagement density and active coverage.
- v2.0.26 should inspect target acquisition, chase routing, and encounter spacing before zone pacing, damage, or retreat-threshold tuning.

## v2.0.26 CHASE Context / Encounter Spacing Probe

- Added persisted doctrine telemetry `chase_context_time_by_archetype`.
- `Bot.gd` tags `CHASE` time as `combat`, `loot`, `recover_loot`, or `unknown`.
- Extended `tools/analyze_results.py` and `tools/compare_scale_profiles.py` with CHASE context mix output.
- Ran fresh 5-run candidate sets:
  - `C:\tmp\game_dev_chase_candidate_60`
  - `C:\tmp\game_dev_chase_candidate_99`
- Kept gameplay unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `simulate_matches.py`, `analyze_results.py`, and `check_scale_telemetry.py`.
- `git diff --check` passed.
- Both fresh 5-run sets passed `check_scale_telemetry.py`.
- Fresh 99 output: avg duration 141.5s, first upgrade 25.4s, legacy disengage 122.4/run, entries 481.8/run, AI avg 518.6us.
- CHASE context mix: 60 is combat 50.0%, loot 29.2%, recover_loot 20.9%; 99 is combat 45.2%, loot 30.9%, recover_loot 24.0%.
- Fresh 99 still does not scale combat throughput with population: entities +64%, damage/match min +8.6%, shots/match min +9.5%, plans/match min -3.3%.

**Decision**

- 99 CHASE time is majority loot/recovery movement, and combat CHASE share drops compared with 60.
- Per-ATTACK efficiency remains intact, so damage tuning is not the first target.
- Reference review corrected the next target: CHASE-to-combat percentage is not a design goal by itself. Battle royale scale should first verify strategic movement, contested routes, and power-position pressure.

## v2.0.27 Strategic Flow / Route Backbone Probe

- Reframed the next 99-player scale question around why entities move and where combat pressure should appear, rather than trying to satisfy a CHASE combat-share target.
- Added candidate-map `routes` data to `data/mapSpec_large_candidate.json`:
  - `west_ridge_choke` and `east_pine_choke` as primary chokes.
  - `north_slope_flank` and `south_creek_flank` as bypass/flank routes.
  - `central_meadow_cross` as loot-flow route.
  - `inner_brush_recovery_exit` as recovery re-entry route.
- Added `MapDefinition.get_route_descriptors()`.
- `MapDefinition.validate()` now checks route id, role, width, point count, point validity, and world bounds when route data exists.
- Added `tools/verify_strategic_flow_map.gd` to guard candidate POI role coverage, route role coverage, primary-choke alternate routes, and route-to-POI references.
- `verify_large_map_candidate.gd` now requires the candidate to expose at least 6 strategic route descriptors.
- Kept gameplay behavior unchanged: no AI aggression, damage, loot, or zone timing changes.

**Verification**

- `verify_map_definition.gd`, `verify_large_map_candidate.gd`, and `verify_strategic_flow_map.gd` passed.
- `git diff --check` passed.

**Decision**

- Keep CHASE context as a diagnostic, not a direct target.
- v2.0.28 should add combat-location / POI-role / route-pressure telemetry before any tuning to AI aggression, damage, pickup spacing, or zone pacing.

## v2.0.28 Combat Location / Route Pressure Telemetry

- Added `MapDefinition.describe_strategic_position()` to classify a world position into:
  - current POI role/name, or `open`.
  - current route role/id, or `off_route`.
  - nearest POI/route metadata for later analysis.
- `Entity.gd` now logs strategic location context for combat damage and combat kills through `Telemetry.gd`.
- Added combat location aggregates:
  - hits/damage/kills by POI role.
  - hits/damage/kills by route role.
  - hits/damage/kills by route id.
- `tools/analyze_results.py` now prints combat-location and route-pressure mixes.
- `tools/compare_scale_profiles.py` now prints route-pressure rows and a `Route pressure decision`.
- Kept gameplay behavior unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd`, `verify_map_definition.gd`, and `verify_candidate_99_probe.gd` passed.
- Candidate-map headless quit passed with only the expected AssetCatalog missing-path warning.
- 1-run candidate `xlarge_60` smoke at `C:\tmp\game_dev_route_pressure_smoke` wrote route-pressure telemetry and `analyze_results.py` printed POI/route mixes.
- `check_scale_telemetry.py --min-runs 1 --min-avg-first-upgrade 0` passed as a schema smoke. The normal 1-run gate failed only because the run upgraded at 1.7s, so it is not counted as a balance pass.
- `git diff --check` passed.

**Decision**

- Route-pressure telemetry is live.
- v2.0.29 should run fresh 5-run candidate `xlarge_60` and `target_99_probe` sets, then compare route pressure before map route, pickup spacing, AI aggression, or zone-pull tuning.

## v2.0.29 Fresh 60-vs-99 Route-Pressure Comparison

- Ran fresh candidate-map 5-run telemetry sets with the post-v2.0.28 combat-location schema:
  - `C:\tmp\game_dev_route_candidate_60_v20260603`
  - `C:\tmp\game_dev_route_candidate_99_v20260603`
- Kept gameplay behavior and map data unchanged.

**Verification**

- `verify_candidate_99_probe.gd`, `verify_large_map_candidate.gd`, and `verify_map_runtime_path.gd` passed.
- Both fresh sets passed `check_scale_telemetry.py --min-runs 5`.
- `xlarge_60`: avg duration 92.7s, first upgrade 10.0s, legacy disengage 84.2/run, AI avg 307.2us, fallback 0.0/run.
- `target_99_probe`: avg duration 140.2s, first upgrade 17.7s, legacy disengage 123.8/run, AI avg 424.0us, fallback 0.0/run.
- `compare_scale_profiles.py` passed on the fresh pair.

**Route-pressure result**

- Combat damage on route: 60 is 63.2%, 99 is 60.7%.
- Combat damage in POI: 60 is 39.7%, 99 is 38.3%.
- Damage route mix stays dominated by `central_meadow_cross` and `inner_brush_recovery_exit`:
  - 60: loot_flow 32.9%, recovery_exit 27.0%, off_route 36.8%, flank 3.0%, primary_choke 0.4%.
  - 99: loot_flow 30.1%, recovery_exit 26.9%, off_route 39.3%, flank 2.7%, primary_choke 1.0%.
- Transit-choke POI damage is effectively absent: 60 is 0.3%, 99 is 0.3%.
- 99 still has diluted engagement coverage: `ATTACK+CHASE` 41.6% -> 38.3%, `RETREAT+ESCAPE` 43.7% -> 47.7%, while damage/ATTACK min improves 758.8 -> 872.3.

**Decision**

- Spawn/pathing and AI budget are still not the current blockers.
- Route telemetry shows the candidate already records many fights near broad movement routes, but not near the intended contested chokepoints, flanks, or transit POIs.
- Do not fix this by forcing CHASE ratios, raw damage, or generic zone speed first.
- v2.0.30 should be a candidate-only strategic-route pressure pass: strengthen primary-choke/flank/power-position incentives and then rerun the same comparison.

## v2.0.30 Candidate Strategic-Route Pressure Pass

- Updated only the candidate map and route-pressure validation/tooling; the default map remains unchanged.
- Candidate map metadata advanced to `3.3-candidate`.
- Reduced Central Meadow radius/density so central `loot_flow` no longer absorbs most nearby combat.
- Added two transit-choke POIs:
  - `West Ridge Overlook` at `[-34, 16]`.
  - `East Pine Gate` at `[42, -6]`.
- Retuned `west_ridge_choke` and `east_pine_choke` route points so those primary routes pass through the new gate POIs.
- Kept only one offset high rock cover per gate after the first smoke showed heavier gate clutter could push stuck triggers over the scale gate.
- `verify_strategic_flow_map.gd` now requires:
  - 4 transit-choke POIs.
  - multi-point primary-choke paths.
  - central route width capped below the old broad catch-all route.
  - exact strategic classification for both new gate POIs.
- `compare_scale_profiles.py` now separates high absolute route/POI pressure from baseline-relative drops in its route-pressure decision text.

**Verification**

- `verify_strategic_flow_map.gd`, `verify_large_map_candidate.gd`, `verify_candidate_99_probe.gd`, and `verify_map_runtime_path.gd` passed.
- Candidate-map headless quit passed for `xlarge_60` and `target_99_probe` with only the expected AssetCatalog warning.
- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- Initial heavy-cover smoke correctly failed the scale gate on stuck triggers, so the map was revised before commit.
- Revised fresh 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_route_gate_candidate_60_v2030`: avg duration 111.7s, first upgrade 10.6s, stuck 42.4/run, fallback 0.0/run.
  - `C:\tmp\game_dev_route_gate_candidate_99_v2030`: avg duration 135.0s, first upgrade 10.1s, stuck 42.6/run, fallback 0.0/run.
- `git diff --check` passed.

**Route-pressure result**

- 99 combat damage on route: 70.1%.
- 99 primary-choke damage: 24.7%, up from 1.0% in v2.0.29.
- 99 transit-choke POI damage: 20.9%, up from 0.3% in v2.0.29.
- 99 damage route mix: primary_choke 24.7%, recovery_exit 23.9%, loot_flow 20.6%, off_route 29.9%, flank 0.9%.
- 60 is more concentrated than 99: route damage 78.7%, POI damage 62.5%, primary-choke damage 23.9%, transit-choke POI damage 29.7%.
- 99 still has thinner active engagement coverage: `ATTACK+CHASE` 39.9% -> 37.8%.
- 99 CHASE context still shifts away from combat: combat 49.5% -> 40.0%, recover-loot 18.6% -> 27.7%.

**Decision**

- The v2.0.29 map-pressure blocker is resolved enough to stop widening primary routes or adding more hard cover.
- More gate clutter is risky because the first smoke pushed stuck triggers above the gate.
- Do not tune raw damage or generic zone speed yet.
- v2.0.31 should inspect CHASE recovery/loot interruptions and POI pressure distribution at 99, then decide whether pickup spacing, recovery exits, objective interrupts, or target acquisition need the next change.

## v2.0.31 CHASE Location / Recovery Target Diagnostics

- Added diagnostic CHASE location telemetry without changing gameplay behavior.
- `Bot.gd` now records CHASE self strategic context, target strategic context, and target kind while CHASE time is logged.
- `Telemetry.gd` persists:
  - `chase_self_poi_role_by_context`.
  - `chase_self_route_role_by_context`.
  - `chase_target_poi_role_by_context`.
  - `chase_target_route_role_by_context`.
  - `chase_target_kind_by_context`.
- `tools/analyze_results.py` now prints CHASE self POI, target POI, target route, and target kind mixes for `combat`, `loot`, and `recover_loot`.
- `tools/compare_scale_profiles.py` now compares CHASE location rows and prints a `CHASE location decision`.
- Kept map data, AI behavior, loot counts, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd`, `verify_large_map_candidate.gd`, and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_chase_location_v2031_smoke` wrote the new telemetry fields and printed CHASE location mixes. The normal single-run scale gate failed only because stuck variance hit 78, so it is not counted as a balance pass.
- Fresh 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_chase_location_60_v2031`: avg duration 113.3s, first upgrade 13.3s, stuck 29.8/run, fallback 0.0/run.
  - `C:\tmp\game_dev_chase_location_99_v2031`: avg duration 140.9s, first upgrade 11.0s, stuck 48.6/run, fallback 0.0/run.
- `compare_scale_profiles.py` passed on the fresh pair and printed the new CHASE location decision.

**CHASE location result**

- 99 still has diluted per-entity combat throughput, but ATTACK-state efficiency is intact:
  - damage/entity/min 31.24 -> 21.34.
  - damage/ATTACK min 790.59 -> 904.17.
  - `ATTACK+CHASE` 40.50% -> 35.86%.
- CHASE context moves modestly away from combat:
  - combat 47.10% -> 42.71%.
  - recover_loot 21.91% -> 24.65%.
- Recover-loot targets remain mostly inside strategic context, but less than 60:
  - target POI 74.90% -> 66.60%.
  - target route 82.64% -> 75.40%.
- Recovery exit remains a strong anchor:
  - recover target recovery_exit 30.08% -> 32.13%.
- Recover-loot target kind is mostly weapon/ammo at 99:
  - weapon 34.43%.
  - ammo 51.36%.
  - heal 7.09%.
  - armor 3.49%.
- Combat CHASE target POI pressure drops at 99:
  - combat target POI 61.42% -> 55.29%.
  - combat target route stays high at 73.46% -> 77.93%.

**Decision**

- Do not widen routes, add more hard gate clutter, tune raw damage, or speed up the zone based on this slice.
- The next candidate-only slice should inspect recovery-exit loot/ammo/weapon spacing and re-entry pressure first.
- Target acquisition around POIs is the secondary suspect because combat CHASE target POI pressure drops while route pressure stays high.
- v2.0.32 should decide whether to adjust recovery-exit pickup placement, ammo economy, or POI target-acquisition rules before touching AI aggression.

## v2.0.32 Pickup Location / Recovery-Exit Source Diagnostics

- Added diagnostic pickup location telemetry without changing gameplay behavior.
- `Pickup.gd` now records successful collection location by item kind.
- `LootSpawnDirector.gd` records spawn location for each pickup after initialization.
- `Telemetry.gd` persists:
  - `pickup_spawn_by_kind`.
  - `pickup_collect_by_kind`.
  - `pickup_spawn_poi_role_by_kind`.
  - `pickup_spawn_route_role_by_kind`.
  - `pickup_collect_poi_role_by_kind`.
  - `pickup_collect_route_role_by_kind`.
- `tools/analyze_results.py` now prints pickup spawn/collect route and POI mixes by `weapon`, `ammo`, `heal`, and `armor`.
- `tools/compare_scale_profiles.py` now compares pickup location rows and prints a `Pickup location decision`.
- Kept map data, bot behavior, loot counts, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_pickup_location_v2032_smoke` wrote the new pickup location fields and passed `check_scale_telemetry.py --min-runs 1 --min-avg-first-upgrade 0`.
- Fresh 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_pickup_location_60_v2032`: avg duration 102.3s, first upgrade 14.3s, stuck 33.4/run, fallback 0.0/run.
  - `C:\tmp\game_dev_pickup_location_99_v2032`: avg duration 153.3s, first upgrade 25.7s, stuck 45.6/run, fallback 0.0/run.
- `compare_scale_profiles.py` passed on the fresh pair and printed the new pickup location decision.

**Pickup location result**

- 99 first upgrade is later in this fresh pair:
  - first upgrade 14.35s -> 25.71s.
  - non-pistol pickups/entity/min 0.06 -> 0.04.
  - rare pickups/entity/min 0.13 -> 0.06.
- Recovery-exit weapon/ammo placement is not a 99-specific concentration:
  - weapon spawn recovery_exit 24.82% -> 21.15%.
  - ammo spawn recovery_exit 26.30% -> 24.48%.
  - weapon collect recovery_exit 30.85% -> 27.41%.
  - ammo collect recovery_exit 32.76% -> 28.94%.
- Weapon/ammo pickup collection loses POI pressure at 99:
  - weapon collect POI 65.25% -> 54.61%.
  - ammo collect POI 68.97% -> 55.51%.
- CHASE target POI pressure also drops:
  - combat target POI 62.78% -> 53.01%.
  - recover target POI 77.60% -> 59.14%.
- Route pressure remains present:
  - combat damage on route 76.52% -> 72.15%.
  - primary-choke damage 21.30% -> 26.50%.

**Decision**

- Do not move recovery-exit loot yet. The pickup location decision says weapon/ammo pressure is distributed enough to inspect POI target acquisition before moving loot.
- The likely next issue is not recovery-exit overstock; it is POI leakage: pickups and CHASE targets are being reached on routes/open areas instead of inside POI influence at 99.
- v2.0.33 should inspect POI target acquisition, route/POI overlap width, and open-area pickup collection before AI aggression, raw damage, generic zone-speed tuning, or candidate-map loot relocation.

## v2.0.33 POI Proximity / Soft-Overlap Diagnostics

- Added diagnostic POI/route proximity bands without changing gameplay behavior.
- `MapDefinition.describe_strategic_position()` now includes:
  - `nearest_poi_radius`.
  - `nearest_poi_edge_distance`.
  - `nearest_route_width`.
  - `nearest_route_edge_distance`.
- `Telemetry.gd` now persists:
  - `chase_self_poi_band_by_context`.
  - `chase_target_poi_band_by_context`.
  - `chase_target_route_band_by_context`.
  - `pickup_spawn_poi_band_by_kind`.
  - `pickup_spawn_route_band_by_kind`.
  - `pickup_collect_poi_band_by_kind`.
  - `pickup_collect_route_band_by_kind`.
- POI bands are `inside`, `near_0_4m`, `near_4_8m`, and `far_8m_plus`.
- Route bands are `on_route`, `near_0_4m`, `near_4_8m`, and `far_8m_plus`.
- `tools/analyze_results.py` prints CHASE and pickup POI/route band mixes.
- `tools/compare_scale_profiles.py` compares soft POI/route coverage and prints a `POI leakage decision`.
- Kept map data, bot behavior, loot counts, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_poi_band_v2033_smoke` wrote the new band fields. The single-run scale gate failed only because stuck variance hit 63.0, so it is not counted as a balance pass.
- Fresh 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_poi_band_60_v2033`: avg duration 128.0s, first upgrade 14.0s, stuck 44.8/run, fallback 0.0/run.
  - `C:\tmp\game_dev_poi_band_99_v2033`: avg duration 152.0s, first upgrade 25.4s, stuck 45.2/run, fallback 0.0/run.
- `compare_scale_profiles.py` passed on the fresh pair and printed the new POI leakage decision.

**POI proximity result**

- Combat CHASE target strict POI drops only moderately:
  - combat target POI 61.16% -> 57.45%.
- Soft POI coverage still drops enough to matter:
  - combat target soft POI 83.93% -> 78.39%.
  - POI leakage decision: combat CHASE targets truly leak away from POI influence.
- Combat target route coverage stays high:
  - combat target route 78.04% -> 74.35%.
  - combat target soft route 93.72% -> 90.42%.
- Recovery target soft POI is more stable:
  - recover target POI 77.85% -> 74.33%.
  - recover target soft POI 86.66% -> 83.19%.
- Pickup collection soft POI drops less than combat target soft POI:
  - weapon collect soft POI 87.02% -> 82.78%.
  - ammo collect soft POI 87.58% -> 83.17%.
- Route combat remains material:
  - combat damage on route 78.41% -> 74.42%.
  - primary-choke damage 24.45% -> 26.47%.

**Decision**

- POI leakage is not only a strict-radius/overlap artifact. Soft POI coverage also falls at target scale.
- The next suspect is combat target acquisition and encounter spacing around POI/route edges, not pickup relocation or route widening.
- v2.0.34 should inspect target acquisition source and target position at acquisition time before AI aggression, raw damage, zone-speed tuning, recovery-exit loot relocation, or POI radius changes.

## v2.0.34 Target Acquisition Source Diagnostics

- Added diagnostic target-acquisition telemetry without changing gameplay behavior.
- `Bot.gd` now routes enemy-target assignment through `acquire_enemy_target(enemy, source_name)` for:
  - idle reaction.
  - post-kill scan.
  - objective interrupt.
  - recover-melee fallback.
  - retreat counteraction.
  - reload/disengage reengage.
  - gunshot lock.
  - damage passive/disengage/switch reactions.
  - peripheral switches.
- `PressureEffectApplier.gd` logs pressure aggro through `acquire_enemy_target()` when available.
- `Telemetry.gd` persists:
  - `target_acquisition_by_source`.
  - `target_acquisition_state_by_source`.
  - `target_acquisition_poi_role_by_source`.
  - `target_acquisition_poi_band_by_source`.
  - `target_acquisition_route_role_by_source`.
  - `target_acquisition_route_band_by_source`.
  - `target_acquisition_distance_by_source`.
- `tools/analyze_results.py` prints target-acquisition source and target POI/route band mixes.
- `tools/compare_scale_profiles.py` compares acquisition count, source mix, acquisition soft POI/route coverage, and prints a `Target acquisition decision`.
- Kept map data, loot counts, bot aggression, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_target_acq_v2034_smoke` wrote the new target-acquisition fields and passed `check_scale_telemetry.py --min-runs 1 --min-avg-first-upgrade 0`.
- Fresh valid 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_target_acq_xlarge60_v2034`: avg duration 104.8s, first upgrade 16.7s, stuck 40.8/run, fallback 0.0/run.
  - `C:\tmp\game_dev_target_acq_99_v2034`: avg duration 148.7s, first upgrade 19.2s, stuck 48.6/run, fallback 0.0/run.
- A discarded `baseline_60` probe produced only 12 spawned entities and failed the gate, so it was not used for the v2.0.34 decision.

**Target acquisition result**

- 99 acquisition-time soft-POI coverage drops moderately:
  - target acquisition soft POI 78.30% -> 73.54%.
  - target acquisition soft route 92.64% -> 88.69%.
- Combat target soft-POI coverage is comparatively stable in the valid fresh pair:
  - combat CHASE target soft POI 81.30% -> 78.20%.
  - combat CHASE target soft route 91.77% -> 89.75%.
- Source mix shifts toward reengage:
  - reengage 38.18% -> 43.08%.
  - scan 23.03% -> 19.63%.
  - noise/damage 17.37% -> 19.36%.
  - objective 18.70% -> 15.46%.
- The weakest active 99 acquisition group is scan:
  - scan soft POI 71.95%, 19.63% of acquisitions.
  - reengage soft POI 73.10%, 43.08% of acquisitions.
  - objective soft POI 73.87%, 15.46% of acquisitions.
- Pickup collection soft-POI coverage is no longer the sharper signal in this pair:
  - weapon collect soft POI 80.54% -> 83.67%.
  - ammo collect soft POI 82.15% -> 83.33%.

**Decision**

- Target acquisition does not show a catastrophic 99-only POI leak from the valid 60 baseline, but acquisition targets are already less POI-centered than desired.
- Targets remain route-bound even when POI pressure drops, so the next suspect is route/POI overlap and encounter spacing around acquisition sources rather than aggression or damage.
- Do not tune AI aggression, raw damage, generic zone speed, recovery-exit loot, or POI radius yet.
- v2.0.35 should inspect route/POI overlap by acquisition source and whether scan/reengage/objective acquisitions happen on route edges outside POI influence.

## v2.0.35 Acquisition Route/POI Overlap Diagnostics

- Added target-acquisition route/POI overlap diagnostics without changing gameplay behavior.
- `Telemetry.gd` now persists:
  - `target_acquisition_overlap_by_source`.
  - `target_acquisition_route_role_poi_band_by_source`.
  - `target_acquisition_nearest_poi_role_by_source`.
  - `target_acquisition_nearest_route_role_by_source`.
- `tools/analyze_results.py` prints acquisition POI/route overlap, route role / POI band, nearest POI role, and nearest route role by source.
- `tools/compare_scale_profiles.py` compares:
  - far-POI but soft-route acquisition.
  - far-POI and on-route acquisition.
  - soft-POI plus soft-route acquisition.
  - source-group far-POI/soft-route shares.
  - route-role far-POI acquisition shares.
- Kept map data, loot counts, bot aggression, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_acq_overlap_v2035_smoke` wrote the new overlap fields and passed `check_scale_telemetry.py --min-runs 1 --min-avg-first-upgrade 0`.
- Fresh valid 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_acq_overlap_xlarge60_v2035`: avg duration 106.0s, first upgrade 15.0s, stuck 27.0/run, fallback 0.0/run.
  - `C:\tmp\game_dev_acq_overlap_99_v2035`: avg duration 174.5s, first upgrade 18.5s, stuck 40.6/run, fallback 0.0/run.

**Acquisition overlap result**

- Target acquisition overlap does not show a large 99-only route/POI leak:
  - far-POI but soft-route acquisition 10.06% -> 10.55%.
  - far-POI and on-route acquisition 3.08% -> 3.61%.
  - soft-POI plus soft-route acquisition 82.18% -> 79.70%.
- Source-group far-POI/soft-route shares are stable:
  - scan 13.00% -> 12.20%.
  - reengage 10.81% -> 11.02%.
  - noise/damage 9.59% -> 9.34%.
  - objective 5.58% -> 9.27%.
- Route-role far-POI acquisition is modest:
  - primary_choke 6.50% -> 7.36%.
  - loot_flow 3.29% -> 2.94%.
  - recovery_exit 0.05% -> 0.01%.
- Acquisition soft coverage remains high:
  - acquisition soft POI 82.61% -> 80.17%.
  - acquisition soft route 92.24% -> 90.25%.

**Decision**

- Route/POI overlap by acquisition source is not the primary blocker. The far-POI/route-bound share is material enough to monitor but too stable to justify route/POI geometry changes by itself.
- The stronger remaining signal is movement interruption after acquisition and during loot/recover CHASE:
  - CHASE combat 48.04% -> 39.85%.
  - CHASE loot 29.65% -> 36.32%.
  - weapon collect soft POI 86.38% -> 81.44%.
  - ammo collect soft POI 88.35% -> 81.58%.
- Do not tune AI aggression, raw damage, generic zone speed, recovery-exit loot, or POI radius yet.
- v2.0.36 should inspect loot/recover CHASE interruption timing, pickup collection pressure, and whether weapon/ammo objectives pull actors off combat rotations.

## v2.0.36 Loot Objective Interruption Diagnostics

- Added loot objective start/outcome telemetry without changing gameplay behavior.
- `Bot.gd` now records loot objective starts for:
  - idle opportunistic loot.
  - post-kill loot.
  - combat low-ammo breakoffs.
  - RECOVER seek-loot.
  - RECOVER patrol-loot.
  - loot retargets.
- `Bot.gd` now records loot objective outcomes:
  - collect.
  - giveup.
  - retarget.
  - invalid target.
  - enemy/gunshot/damage/zone/state interrupts.
- `Telemetry.gd` persists:
  - `loot_objective_start_by_source`.
  - `loot_objective_mode_by_source`.
  - `loot_objective_origin_state_by_source`.
  - `loot_objective_kind_by_source`.
  - `loot_objective_target_poi_band_by_source`.
  - `loot_objective_target_route_band_by_source`.
  - `loot_objective_start_distance_by_source`.
  - `loot_objective_outcome_by_source`.
  - `loot_objective_outcome_by_kind`.
  - `loot_objective_duration_by_source`.
  - `loot_objective_duration_by_outcome`.
- `tools/analyze_results.py` prints loot objective source, mode, kind, target POI band, outcome, and duration mixes.
- `tools/compare_scale_profiles.py` compares loot objective counts, source mix, outcome mix, target soft POI, duration, and prints a `Loot objective decision`.
- Kept map data, loot counts, bot aggression, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `compare_scale_profiles.py`, `analyze_results.py`, `simulate_matches.py`, and `check_scale_telemetry.py`.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_loot_objective_v2036_smoke` wrote the new loot objective fields and passed `check_scale_telemetry.py --min-runs 1 --min-avg-first-upgrade 0`.
- Fresh valid 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_loot_objective_xlarge60_v2036`: avg duration 96.6s, first upgrade 13.8s, stuck 33.0/run, fallback 0.0/run.
  - `C:\tmp\game_dev_loot_objective_99_v2036`: avg duration 156.4s, first upgrade 12.0s, stuck 58.8/run, fallback 0.0/run.

**Loot objective result**

- Loot/recover CHASE is high but not the largest 99 delta in this pair:
  - CHASE combat 46.12% -> 42.08%.
  - CHASE loot 29.18% -> 33.09%.
  - CHASE recover_loot 24.70% -> 24.83%.
- Loot objective target mix is mostly weapon/ammo and becomes more so at 99:
  - weapon/ammo objective share 81.85% -> 86.82%.
  - heal/armor objective share 18.15% -> 13.18%.
- Source mix is stable:
  - idle source 65.11% -> 64.66%.
  - combat low-ammo source 32.15% -> 31.73%.
  - recover seek source 0.00% -> 0.20%.
- Outcomes improve rather than degrade at 99:
  - collect 25.99% -> 31.34%.
  - interrupt 65.09% -> 58.24%.
  - giveup 0.22% -> 0.30%.
  - avg duration 0.56s -> 0.57s.
- Objective target soft-POI coverage is stable:
  - 83.56% -> 85.36%.
- Recovery-exit weapon/ammo pressure appears in this pair:
  - weapon collect recovery_exit 22.00% -> 27.93%.
  - ammo collect recovery_exit 23.05% -> 28.87%.

**Decision**

- Loot objective telemetry does not support a pathing/timeout/giveup problem. Objectives are short, mostly collected or interrupted quickly, and collection improves at 99.
- The main pressure is weapon/ammo objective selection and recovery-exit access, not generic loot movement.
- Combat low-ammo breakoffs are material enough to inspect before aggression or damage tuning.
- Do not tune AI aggression, raw damage, generic zone speed, or POI radius yet.
- v2.0.37 should inspect ammo access/objective selection thresholds, combat low-ammo breakoff criteria, and recovery-exit weapon/ammo pressure before moving loot or changing combat aggression.

## v2.0.37 Ammo Objective Selection Diagnostics

- Added loot-objective selection context without changing gameplay behavior.
- `Bot.gd` now records objective need, ammo band, reserve band, current weapon, target detail, and target match at loot objective start.
- `Telemetry.gd` persists:
  - `loot_objective_need_by_source`.
  - `loot_objective_ammo_band_by_source`.
  - `loot_objective_reserve_band_by_source`.
  - `loot_objective_weapon_by_source`.
  - `loot_objective_target_detail_by_source`.
  - `loot_objective_target_match_by_source`.
  - `loot_objective_target_poi_role_by_source`.
  - `loot_objective_target_route_role_by_source`.
  - `loot_objective_route_kind_by_source`.
  - `loot_objective_route_detail_by_source`.
- `tools/analyze_results.py` prints objective-selection need/ammo/reserve/detail/match/route mixes.
- `tools/compare_scale_profiles.py` compares same-weapon ammo, ammo mismatch, weapon replacement, reserve-empty, low-ammo need, and recovery-exit objective pressure, then includes those signals in `Loot objective decision`.
- Kept map data, loot counts, bot aggression, damage, and zone pacing unchanged.

**Verification**

- Python compile checks passed for `analyze_results.py`, `compare_scale_profiles.py`, and `check_scale_telemetry.py`.
- `git diff --check` passed.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_ammo_objective_v2037_smoke` completed and wrote the new objective-selection fields.
- Fresh valid 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_ammo_objective_xlarge60_v2037`: avg duration 107.0s, first upgrade 12.3s, stuck 32.0/run, fallback 0.0/run.
  - `C:\tmp\game_dev_ammo_objective_99_v2037`: avg duration 162.8s, first upgrade 20.4s, stuck 38.4/run, fallback 0.0/run.

**Ammo objective result**

- 99 scale still shows an engagement-density problem rather than an ATTACK lethality problem:
  - ATTACK+CHASE 41.28% -> 36.65%.
  - damage/ATTACK min 761.2 -> 892.6.
  - shots/ATTACK min 90.9 -> 110.0.
- Loot objective target mix becomes more weapon/ammo-heavy at 99:
  - weapon/ammo objective share 80.74% -> 87.47%.
  - heal/armor objective share 19.26% -> 12.53%.
- Reserve pressure is nearly universal:
  - reserve empty 97.56% -> 97.85%.
  - ammo empty/low 49.36% -> 47.63%.
  - explicit empty/no-reserve need remains low, 0.13% -> 0.62%.
- Ammo mismatch is material:
  - ammo same weapon 30.17% -> 33.52%.
  - ammo mismatch 10.65% -> 13.10%.
- Recovery-exit objective pressure remains material but is not the only scale delta:
  - recovery-exit objective target 39.60% -> 32.23%.
  - recovery-exit weapon+ammo 29.78% -> 26.06%.
- Objective outcomes still do not suggest pathing/giveup as the main blocker:
  - collect 27.08% -> 36.26%.
  - interrupt 66.90% -> 56.00%.
  - avg duration 0.58s -> 0.53s.

**Decision**

- The next work should be a narrow gameplay-data/selection pass, not broad aggression or damage tuning.
- First candidates:
  - prefer same-weapon ammo harder when reserve is empty or current ammo is low.
  - make combat low-ammo breakoffs reserve-aware so bots with sufficient reserve do not abandon pressure too early.
  - inspect whether recovery-exit and primary-choke pickup pressure should be redistributed by type rather than by raw count.
- Do not promote default/global 99, tune generic zone speed, expand POI radius, or adjust combat aggression before this selection pass is tested.

## v2.0.38 Ammo Objective Selection Tuning

- Applied a narrow Bot loot selection tuning pass.
- `Bot.gd` changes:
  - combat low-ammo looting now requires empty reserve ammo.
  - unusable mismatched ammo is skipped during pickup scoring.
  - same-weapon ammo gets priority only when reserve is empty and magazine ammo is empty/low.
  - pistol users give bounded priority to non-pistol weapon pickups so ammo preference does not starve upgrades.
- Kept map data, loot counts, bot aggression, damage, zone pacing, and default/global 99 promotion unchanged.

**Verification**

- `git diff --check` passed.
- `verify_strategic_flow_map.gd` and `verify_candidate_99_probe.gd` passed.
- 1-run 99 smoke at `C:\tmp\game_dev_ammo_tuning_v2038c_smoke` completed after final tuning.
- Fresh valid 5-run sets passed `check_scale_telemetry.py --min-runs 5`:
  - `C:\tmp\game_dev_ammo_tuning_xlarge60_v2038c`: avg duration 108.4s, first upgrade 11.4s, stuck 27.6/run, fallback 0.0/run.
  - `C:\tmp\game_dev_ammo_tuning_99_v2038c`: avg duration 157.4s, first upgrade 21.1s, stuck 38.2/run, fallback 0.0/run.

**Ammo tuning result**

- 99 vs v2.0.37:
  - same-weapon ammo objective share 33.52% -> 53.09%.
  - ammo mismatch 13.10% -> 10.17%.
  - combat low-ammo objective source 28.17% -> 25.93%.
  - explicit empty/no-reserve need 0.62% -> 0.09%.
  - non-pistol pickups/entity/min 0.12 -> 0.13.
- Combat and acquisition placement improved at 99:
  - combat target soft POI 78.85% -> 83.56%.
  - target acquisition soft POI 76.57% -> 79.20%.
  - pickup collect weapon soft POI 81.77% -> 85.74%.
  - pickup collect ammo soft POI 82.02% -> 83.39%.
- The tradeoff is objective reliability:
  - collect 36.26% -> 31.08%.
  - interrupt 56.00% -> 60.92%.
  - avg objective duration remains short, 0.53s -> 0.50s.
- v2.0.38 60 -> 99 still has engagement-density loss:
  - ATTACK+CHASE 42.27% -> 37.69%.
  - damage/ATTACK min 681.7 -> 857.3.
  - CHASE combat 48.15% -> 42.13%.

**Decision**

- Keep the narrow ammo selection tuning. It improves same-weapon ammo targeting and several POI/target-acquisition placement signals without breaking scale gates.
- Do not continue with broad aggression, raw damage, generic zone speed, or POI radius tuning yet.
- v2.0.39 should inspect the remaining ammo mismatch source and objective interruption/enemy-acquisition timing. Add joint target detail/match/source telemetry if needed before changing more behavior.

## Planned Follow-Ups

- v2.0.39: inspect remaining ammo mismatch source and loot objective interruption/enemy-acquisition timing after the ammo selection tuning.
- Later scale presets: 11 -> 24 -> 40 -> 60 -> 99 with AI LOD and telemetry checks at each step.

# v1.9-dev Summary

> Compact summary extracted from the full devlog. See [DEVLOG_full_2026-05-13.md](DEVLOG_full_2026-05-13.md) for exact historical entries.

## Audio Catalog Hook Pass

- Player and bot weapon fire routes use catalog-aware sound IDs such as `shoot.pistol`, `shoot.ar`, `shoot.shotgun`, and `shoot.railgun`.
- Player footsteps classify grass/dirt/stone surfaces and call catalog footstep IDs.
- Fallback procedural sounds remain active when files are missing.

## Cosmetic Tint Catalog Hook

- Player and bot archetype body/accent colors read from AssetCatalog cosmetic tint entries.
- Existing primitive visual construction remains in code.

## Missing Path Summary

- AssetCatalog now tracks configured paths that do not exist.
- Debug overlay can show missing asset count.
- Missing asset paths warn but keep fallback behavior active.

## Scale Test CLI Overrides

- Added zone timing/damage override args:
  - `zone_wait=`
  - `zone_shrink=`
  - `zone_dps=`
  - `zone_initial=`
- `tools/simulate_matches.py` passes extra `key=value` args through to Godot autostart.

## Debug Flag Hooks

- Added shared `Main.debug_enabled(flag)` and `debug_log(flag, message)` helpers.
- Added logs for damage, perception, loot, and zone state.

## Verification

- Godot headless quit passed during the original work.
- One-run simulations passed after each narrow slice.
- `git diff --check` passed during the original work.
